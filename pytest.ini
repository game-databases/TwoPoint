[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    client_gated: needs TPC_GAME_DIR or the default install path; auto-skips loudly when absent
    heavy: real-corpus stages 1-5; additionally requires TPC_IT_HEAVY=1
    slow: optional long-running leg (full-pipeline double-run)
