# verifyB — adversarial verification of [scout-report-piece-06-media.mdx](../scout-report-piece-06-media.mdx)

**Agent:** verifyB-piece-06 (adversarial seat) · **Date:** 2026-08-26 · **Host:** NE8K ·
**Target:** [`docs/scout-report-piece-06-media.mdx`](../scout-report-piece-06-media.mdx) ·
**buildId under audit:** 20226581 (unchanged — no client movement this session).

Method: every attack was executed against disk, never argued from tone.
Re-measurements ran in a scratch workspace (`D:\tpc_pytmp\vb06`, deleted
after this write) using the pack `.venv` (UnityPy 1.25.3, Pillow 12.3.0),
the landed artifacts under `extracted/`, and fresh header/pixel reads of
the install at `A:\SteamLibrary\steamapps\common\Two Point Campus`. No
binary image was read into context — PIL stats and byte counters only.
Nothing was committed; the only repo write is this file.

Inputs read in full first: repo-root `AGENTS.md`,
`_foundation/decision-register.md`, `_foundation/extraction-doctrine.md`,
`_foundation/extraction-host.md`, `promptForDB.md`, pack `spec.md`,
`README.md`, `data-acquisition.md`, `toolchain.md`,
[scout-report-001 §toolchain](../scout-report-001.mdx), and the report
under verification.

---

## Attack log

### A1 — The 99.7% join (denominator, dedup, case-sensitivity)

**Result: headline SURVIVES exactly; several supporting cells do not reconcile.**

Independent re-walk of all nine stub kinds (recursive over `fields.*`,
filter `m_SubObjectType startswith UnityEngine.Sprite`, resolution =
catalog guid → `container_index` class → catalogue Sprite-name index)
reproduces the report's headline **exactly**:

| Quantity | Report | This audit |
|---|---|---|
| Distinct referenced sprite names | 2,158 | **2,158** |
| Resolved via catalogue name index | 2,151 (99.7%) | **2,151 (99.676%)** |
| Unmatched set | 7 DLC3 names | **same 7 names** |

The report's denominator is *distinct sprite-typed `m_SubObjectName`
values regardless of target class*; its numerator is presence of the
name among catalogue Sprite rows. Both re-derived. Case-folding finds no
additional hits (case-insensitive-only matches: 0), so no dedup artifact
inflates the rate.

Per-kind reconciliation of the §3 table:

- `rows` column: **9/9 exact**. `rows w/ any GUID ref`: **9/9 exact**
  (items 3,875 · configs 3,406 · metagame-nodes 344 · rooms 106 ·
  student-types 27 · staff 3 · unlockables 124 · courses 0 ·
  campus-levels 13 — all re-measured identical).
- Resolve column: items 1,919 ✓ · metagame-nodes 202 ✓ · rooms 115 ✓ ·
  student-types 27 ✓ · staff 9 ✓ · unlockables 33 ✓. **configs 1,565 ✗
  NOT REPRODUCED**: I measure 1,423 sprite-typed configs refs, 1,412 of
  them targeting a SpriteAtlas. No filter tried (any-type, field-name
  regexes, relink-file variants) yields 1,565. Delta +142..153 unexplained.
- Dangling column: semantics differ (their distinct-GUID count vs my
  unique `(fieldPath, guid)` pairs): items 363 vs 371, unlockables 26 vs
  17, metagame-nodes 0 vs 2, rooms 0 vs 1. The two extra metagame-node /
  room danglings are DLC3 icon-shaped fields (`IconReference`,
  `_iconReference`) — so "zero dangling among genuine icon fields"
  has concrete exceptions beyond `EditorFallbackIconReference`.
- **"All 3,870 sprite-typed references land on `.spriteatlasv2`" is
  overstated twice.** First, the 3,870 figure inherits the unreconciled
  configs cell (true sprite-typed total: 3,728). Second, 11 sprite-typed
  configs refs target something else: 3 × lone `Sprite` objects,
  2 × `Texture2D`, 6 × catalog-GUID-only entries with no container row
  (the un-owned DLC3 content, GUID `dd036a35c23b…`). Correct statement:
  3,717/3,728 = 99.7% of sprite-typed refs target an atlas asset.

**Wider-universe attack (icons outside the stub fields).** Swept all
167,069 monobehaviour dumps; 3,540 files carry sprite-typed GUID refs
holding 2,687 distinct subObjectNames — 529 beyond the stub universe,
of which 525 exist in the catalogue. Whole-corpus dump-level rate:
**2,676/2,687 = 99.59%**. Expanding the universe does not materially
drop the rate; four new missing states appear (empty string + three
`ANIM_M/F_Advisor_Chat_*` values typed as Sprite subtype).

**Cross-bundle placement (new fact the export stage needs).** For
**2,059 refs covering 820 distinct names**, the named Sprite object does
NOT live in the bundle of the atlas its GUID targets. Homes:
`ui-icons-atlased-assets_assets_all.bundle` (960 refs), `ui-art-icons`
(593), `dlc-space-ui` (271), `dlc-ghost-ui` (115), `ui-art-ingame` (93),
others small. Verified concretely: `AdvisorHuski` (93 refs → atlas
`UI_Icons.spriteatlasv2` in `ui-spriteatlas_assets_all.bundle`) exists
only in `ui-icons-atlased-assets_assets_all.bundle`. Resolution-by-name
still works everywhere; what breaks is any exporter assumption that
"name → sprite inside the targeted atlas's bundle".

### A2 — Atlas-crop assumption (counterexample hunt)

**Result: SURVIVES in substance; three special cases added.**

- Page topology claims verified against the 125 `sactx-*` Texture2Ds:
  naming scheme exact; largest pages 4096×4096 DXT5 exact;
  `UI_Items_New` = 3×4096² + 4096×2048 tail exact; `UI_Icons` =
  4096² + 2048×4096 exact. One correction: **`UI_Costumisation` is
  4096² + 2048², not "two 4096²"**. Exactly one page corpus-wide is BC7
  (`sactx-0-2048x2048-BC7-UI_MenuCommon-…`); it carries ≤1 referenced
  name, so BC7 decode is a footnote-level special case, not a lane.
- **3,140 of 6,789 sprites have a NULL `m_RD.texture` pointer** (all
  1,650 sprites in `ui-spriteatlas_assets_all.bundle`; the
  `ui-icons-atlased-assets` population). Cropping them requires going
  through their atlas's `m_RenderDataMap`; the Sprite object alone is
  insufficient. §4's "crop by per-sprite rect … fully available" holds
  only with this route spelled out.
- Pairing rule established empirically: `m_RenderDataMap` entries are
  **not** index-aligned with `m_PackedSpriteNamesToIndex` (0/352 rect
  agreement on `UI_Icons`). The working rule is: take the Sprite
  object's own `m_RD.textureRect` SIZE and match it against renderdata
  entries — validated pixel-exact (0 differing pixels, maxDelta 0)
  against independent AssetStudioModCLI exports for both probe sprites
  (`DLC2_Qualifications_janitor_SML` 100×116, `_LRG` 211×243).
- 3,460 sprites carry fractional rects (e.g. AdvisorHuski 337.93×509.97)
  — an explicit rounding policy is needed; not mentioned in the report.
- Standalone route confirmed: 492 sprites cover their entire own texture
  (pass-through candidates); "≈55% of sprites live outside atlases"
  survives (I measure 53.5% = 3,635/6,789 non-packed). `m_IsVariant=false`
  on all 47 atlases confirmed.
- **§2 sprite-population stats NOT REPRODUCED:** report says 4,686/6,789
  sprites have a usable rect (≥4 px both axes) with ladder
  ≤64px=1,477 · ≤128=2,582 · ≤256=3,712 and median area 8,192 px². My
  sweep: **6,787/6,789** usable (identical via `textureRect` and
  `m_Rect`), ladder ≤64=647 · ≤128=2,684 · ≤256=5,955, median area
  **34,939 px²**, corpus-wide median max-dim 206 px. No subset or
  failure-mode filter I tried reconstructs 4,686.

### A3 — "Courses carry zero icon references"

**Result: the hole is REAL; three corrections around it.**

- Field census over all 69 course stubs: zero visual fields. The 69 rows
  split **28 `TPC.CourseDefinition` + 41 `TPC.MarketingCourseDefinition`**;
  neither class carries any icon/emblem/sprite/art field (only false
  positive: `StartPointsCost` matching a too-loose regex). Claim survives.
- Correction 1: "**Qualification PPtr on all 69 course rows**" is wrong —
  present on **28/69** only (the MarketingCourse rows lack it).
- Correction 2: name-convention yield re-measured strictly: 9 of the 28
  `Course_*` ids hit `UI_InGame_Courses_T_Icon_X` sprite names (report:
  11/69 under its looser framing — same conclusion, tiny either way).
  The **42 existing `*_Courses_T_Icon_*` sprites** reconciles exactly.
- Sharpened carrier evidence (makes §9 Q1 cheaper than implied): 24
  config-side GUID refs already point into the
  `Courses-UI_InGame_T_Spritesheet_Icons_Qualifications_Courses_*` sprite
  family, from rows including `Qualification_T_{Gastronomy,Clown,Goth,…}_Lite`,
  `Course_KnightSchool_Lite`, and CampusEvent/Challenge definitions. The
  mapping probe is a bounded configs join, already half-landed via
  [relinks/course_config.jsonl](../../extracted/relinks/course_config.jsonl)
  (652 edges).

### A4 — Format exclusivity ("no ASTC/ETC2")

**Result: SURVIVES — tested corpus-wide, not sampled.**

Full header sweep of all **176** bundles (base aa + both DLC dirs,
`catalog.bundle` excluded): formats are exactly DXT1 4,504 · DXT5 2,793 ·
RGBA32 141 · RGB24 64 · BC4 314 · BC7 90 · Alpha8 71. Zero ASTC, zero
ETC2, anywhere — DLC and UI bundles included. Numeric correction to the
§2 table: **DXT5 is 2,793 objects / 19,527,408 B, not 2,799 / 19,528,904**.
My sweep total serialized = **209,500,708 B = the landed catalogue
value**; the report's Total row (209,502,204) and its "+1,496 B delta
between my sweep and the catalogue" attribution are backwards — the
landed catalogue is right and the report's sweep carried the +6 phantom
DXT5 textures. Immaterial to conclusions; wrong as stated.

### A5 — Volume estimates

**Result: arithmetic internally consistent; the input AREA is wrong ×4.08, so the floors are re-based.**

- §3.4 "Measured icon sizes for the referenced set": **BROKEN.** Median
  max-dim 42 px, ≤64px = 1,509 (70%), >512px = 41, total area
  26,595,109 px — none of these reproduce under either authoritative
  rect source (Sprite-object rects, cross-validated pixel-exact against
  AssetStudioModCLI; or atlas renderdata rects). Correct values over the
  same 2,151 rect-known names: **total crop area 108,445,749 px**,
  median max-dim **≈233 px**, ≤64px = **29 (1.3%)**, ≤128px = 443
  (20.6%), ≤256px = 2,015 (93.7%), >512px = **4** (the largest being the
  2048² `TPC_DLC2_Student_Ghost` loading-screen focus image).
- Bits-per-pixel assumptions VALIDATED empirically: eight real crops
  (standalone pass-through, shared-sheet subrect, and atlas-page routes)
  encoded through Pillow 12.3.0 measure **WebP q80 lossy 0.54–1.91 bpp,
  WebP lossless 0.82–4.56 bpp, PNG 1.64–7.74 bpp** — the report's
  assumed bands were reasonable.
- Re-based shipped volumes over 108,445,749 px: **WebP q80 ≈ 8–25 MiB**
  (not 1.7–5), lossless ≈ 10–60 MiB, PNG ≈ 21–100 MiB. The decision
  reading survives — megabytes, not gigabytes — but the floor moves ~5×,
  and §5's ladder rationale gets STRONGER (42 px sources don't exist;
  native icons center near 233 px, so a 16/32/64 ladder is even more
  clearly destructive).
- Exact replacements for the ESTIMATE rows (same cpp table, measured
  per-texture): resident GPU payload **4,816,753,294 B ≈ 4.49 GiB**
  no-mip (report est. 4.32 GiB), **6,171,504,655 B ≈ 5.75 GiB**
  mip-inclusive (report est. 5.52 GiB), Σ(w·h) = 6,724,359,442 px,
  RGBA-equivalent 25.05 GiB (report 25.12 ✓ within rounding).
- Row "on-disk size of the 96 texture-bearing bundle FILES":
  **2,420,511,413 B — exact reproduction**, and tex-bearing bundle count
  = 96 exact; corpus = 176 bundles exact (177 `.bundle` files minus
  `catalog.bundle`).

### A6 — Tooling hands-on

**Result: SURVIVES — flags behave as cited, exercised end-to-end.**

- `AssetStudioModCLI.exe --help` runs; banner **v0.19.0 net8 portable** ✓.
  Cited flags exist verbatim: `-t sprite,tex2d`, `-m
  info/export/exportRaw/dump`, `--image-format none|jpg|png|bmp|tga|webp`,
  `-g containerFull`, `-f assetName_pathID`, `--export-asset-list`,
  `--unity-version <text>`, `--filter-by-name/-pathid/-container`.
- Tiny single-file exports into the scratch dir succeeded with
  `--unity-version 2020.3.47f1`: one PNG and one **WebP** sprite
  (`--image-format webp` proven, RGBA output opened and measured via PIL).
- `--unity-version` necessity CONFIRMED: without a real version the
  loader falls back loudly (`UnityVersionFallbackWarning`, header
  `0.0.0`) — matching the executor-001 finding the report cites.
- Gap 2 ("CLI cannot express the join") proven empirically: filtering a
  referenced sprite name that lives outside the atlas bundle returns
  `Found [0/1650]` — the CLI sees only per-bundle objects.
- UnityPy note the next builder needs: setting the `FALLBACK_UNITY_VERSION`
  environment variable alone is insufficient in 1.25.3 —
  `UnityPy.config.FALLBACK_UNITY_VERSION` must be assigned
  ([tools/unitypy_util.py](../../tools/unitypy_util.py) does this
  correctly). Full 176-bundle Texture2D/Sprite/SpriteAtlas header sweep:
  ~30 s wall, zero object errors (report said ~15 s for 96 bundles —
  same order, immaterial).
- Codec/host row verified live: ffmpeg 7.1 gyan build on PATH ✓;
  `cwebp`/`avifenc` absent ✓; `pillow_avif` import absent ✓; Pillow WebP
  encode exercised ✓. Its caution also aged WELL: C: free space is now
  **~0.07 GiB** (was 84.77 GiB at last record) — re-measure-before-bulk-
  decode was the right instruction, and bulk decode belongs on D:/A:.

### A7 — Provenance audit (numbers traced to artifacts)

| # | Claimed | Artifact check | Verdict |
|---|---|---|---|
| 1 | media-catalogue.jsonl 47,939 rows | wc + parse = 47,939 | EXACT |
| 2 | totalBytesEstimate 1,068,259,162 B | Σ bytesEstimate = 1,068,259,162 | EXACT |
| 3 | Per-class counts (AnimationClip 7,985 … Texture2D 7,977, SpriteAtlas 47, VideoClip 31) | recount 9/9 | EXACT |
| 4 | export-manifest.jsonl 167,069 rows | wc = 167,069 | EXACT |
| 5 | catalog.json 54,143 distinct guids; container_index 49,855 addresses; entity_asset_guid 15,630 rows | recount 54,143 / 49,855 / 15,630 | EXACT ×3 |
| 6 | Census reconciliation 7,977 Texture2D + 6,789 Sprite + 47 SpriteAtlas | full sweep, zero residue | EXACT |
| 7 | 96 tex-bearing bundles = 2,420,511,413 B | recount both | EXACT |
| 8 | Per-kind rows / rows-with-GUID-ref (18 cells) | recount 18/18 | EXACT |
| 9 | items/metagame/rooms/student-types/staff/unlockables resolve cells | recount 6/6 | EXACT |
| 10 | configs resolve 1,565; "all 3,870 land on atlas"; "zero dangling icon fields" | measured 1,412–1,423 / 3,717 of 3,728 / exceptions exist | NOT REPRODUCED |
| 11 | §3.4 size cluster (median 42px, 26,595,109 px, >512=41) | two independent rect sources | NOT REPRODUCED (A5) |
| 12 | §2 sprite cluster (4,686 usable, ladder, median 8,192 px²) | sweep | NOT REPRODUCED (A2) |
| 13 | DXT5 2,799 / Total 209,502,204 | sweep + catalogue say 2,793 / 209,500,708 | OFF BY +6 / +1,496 B |
| 14 | Cubemap 152 / Texture2DArray 19 container hits | census `objectsByClass`: **138 / 9**; catalog-address contains-cubemap: 162 | NOT REPRODUCED (use census) |
| 15 | Duplicate atlas objects `UI_DLC1_Items`/`DLC1_Space_UI_Icons` ×2 (§4 table, gap 3, Q5/Q3) | 47 catalogue rows, 0 same-name duplicates; sweep agrees | NOT REPRODUCED — gap dissolves |
| 16 | 42 `*_Courses_T_Icon_*` sprites; packed slots 3,154; UI_Items_New 1,099 / Costumisation 400 / Icons 352 / MenuCommon 131 / HUD 128 | recount | EXACT |
| 17 | items plain-PPtr icon slots null "on the sampled rows" | corpus: 683 null / **24 non-null external** (A3) | TRUE AS WORDED, incomplete |

Silent-scope-cut check: all seven briefed attack surfaces were worked;
none skipped. Two probes were narrowed rather than run to completion and
are declared here: (a) chasing external fileId 14 of
`dlc-ghost-configs_assets_all.bundle` stopped once it resolved to an
inner CAB path (`archive:/CAB-633e…`) — establishing that the 24
non-null PPtr slots need dependency-walking, not whether each lands on
art; (b) the renderdata pairing rule was validated on two sprites
against an independent tool, not on all 3,154 slots.

---

## Verdict table

| # | Report claim | Verdict |
|---|---|---|
| 1 | Headline: 2,151/2,158 distinct referenced sprites resolvable (99.7%) | **SURVIVED** — reproduced exactly; wide-universe rate 99.59% |
| 2 | Join ladder drives off landed artifacts (`entity_asset_guid` + indexes) | **SURVIVED** — recomputed independently from both stubs and the relink file |
| 3 | "All 3,870 sprite-typed refs land on `.spriteatlasv2`, 100%, zero dangling icon fields" | **DOWNGRADED** — 3,717/3,728 atlas-targeted; 11 Sprite/Texture2D/DLC3-only exceptions; configs cell 1,565 unreconciled (measure 1,412–1,423); dangling exceptions exist |
| 4 | The 7 unmatched are DLC3/Medical-School content | **SURVIVED with correction** — set exact, but Cheeseball is a stale name in an unused BASE-game event config targeting an installed atlas (6/7 DLC3) |
| 5 | Plain PPtr icon slots are dead weight; exporter ignores bare PPtr slots | **DOWNGRADED** — true for 683/707 item slots, but 24 slots are non-null EXTERNAL pointers (fileId 14) with empty paired References; ignoring them silently drops those rows' icons — ledger them |
| 6 | §3.4 measured icon sizes (median 42 px; ≤64 = 70%; area 26,595,109 px; >512 = 41) | **BROKEN** — correct values from cross-validated rects: area 108,445,749 px, median ≈233 px, ≤64 = 1.3%, >512 = 4 |
| 7 | §2 format table + "no ETC2/ASTC" | **SURVIVED with correction** — corpus-wide confirmed; DXT5 2,793 not 2,799; catalogue total is the correct one |
| 8 | §2 sprite population (4,686 usable rects; ladder; median 8,192 px²) | **BROKEN** — 6,787/6,789 usable; different ladder; median 34,939 px² |
| 9 | §2 dimensions/mips (top dims, 29 zero-size font atlases, UI single-mip split) | **SURVIVED** — top dims exact; non-pow2 356 = 327 + the 29 zero-size rows; mip-split denominators don't sum (7,983 ≠ 7,977) but substance holds |
| 10 | §4 atlas inventory (pages, topology, 3,154 slots, ~55% unpacked, no variants) | **SURVIVED** — page facts exact; corrections: Costumisation = 4096²+2048²; one BC7 page (≤1 referenced name) |
| 11 | Duplicate same-name atlas objects exist (gap 3 / Q5) | **BROKEN (reversed)** — 0 duplicates among the 47 rows; the dedupe-rule gap dissolves |
| 12 | Courses carry zero icon references; FEASIBLE-AFTER-MAPPING | **SURVIVED** — zero visual fields on all 69 rows; carrier evidence sharpened (24 config-side refs already point at the Courses-Qualifications sprite family); Qualification PPtr is 28/69, not 69/69 |
| 13 | §5 spec inputs (WebP primary, AVIF NOT-NOW, native-res ladder, subObjectName naming, slug-later) | **SURVIVED** — encoder/support claims exercised; native-res argument stronger post-A6 |
| 14 | §6 tooling readiness (AssetStudioModCLI flags, UnityPy path, stage pattern, codec/host) | **SURVIVED** — hands-on proven; additions: `UnityPy.config.FALLBACK_UNITY_VERSION` must be set (env var alone insufficient); C: free space now ~0.07 GiB |
| 15 | §7 volume estimates (1.7–5 MiB WebP floor etc.) | **DOWNGRADED** — bpp assumptions validated, input area ×4.08 too low: re-base to ≈8–25 MiB q80 / 10–60 lossless / 21–100 PNG; multi-GiB context rows replaced with exact figures |
| 16 | §8 feasibility verdicts (FEASIBLE-NOW core, TARGETED-ONLY world textures, OUT-OF-SCOPE generative) | **SURVIVED** — every row stands at corrected magnitudes |
| 17 | §9 gap ledger (course carrier, unlockable routing, editor-only fallbacks, slug dependency, DLC3 owner item) | **SURVIVED** — Q3/Q5 dissolve (no duplicate atlases); Q1 cheaper than implied; rest stands |

## Carry-forward corrections for the export spec (freeze-blocking)

1. Re-base §3.4/§7 on the measured rect set: 108,445,749 px source area,
   median max-dim ≈233 px; shipped floors ≈8–25 MiB (q80). Ladder sizing
   follows the corrected distribution.
2. Exporter routes: (a) standalone pass-through (492 sprites); (b)
   direct-pointer subrect (3,157); (c) atlas-bound with NULL texture
   pointer (3,140) — crop via owning atlas's renderdata matched by the
   Sprite's own textureRect SIZE (index alignment is provably wrong);
   fractional rects (3,460) need a stated rounding rule.
3. Drop the duplicate-atlas dedupe rule; add instead a
   same-bundle-miss handler: resolve sprites BY NAME across bundles
   (2,059 refs / 820 names live outside their targeted atlas's bundle).
4. Ledger, don't drop: the 24 non-null external PPtr icon slots, the 11
   non-atlas-targeted sprite-typed refs, and the 4 new wide-universe
   missing states (3 `ANIM_*Advisor_Chat*` mis-typed + empty-string).
5. Replace Cubemap/Texture2DArray counts with census values 138/9 when
   the type-filter decision is written.

## Closing verdict

**PASS-WITH-CORRECTIONS** — the report's load-bearing feasibility claims
(the 99.7% join, Pillow-decodable corpus, megabyte-scale deliverable,
toolchain readiness, course/unlockable mapping holes) all survive
adversarial re-measurement, most of them to the exact digit; the
corrected items above must land in the piece-06 spec before freeze,
chiefly the ×4.08 source-area correction and the three-route crop
mechanics.
