# Verify B — adversarial verification — scout-report-piece-08-search

**Verifier:** verifyB (adversarial seat; judges, does not fix; spawned no
subagents). **Date:** 2026-08-26.
**Subject:** [`../scout-report-piece-08-search.mdx`](../scout-report-piece-08-search.mdx)
(read to its §END sentinel).
**Requirement set read in full:** repo-root `AGENTS.md`,
`_foundation/decision-register.md`, `_foundation/extraction-doctrine.md`,
`promptForDB.md`; pack `spec.md`, `README.md`, `data-acquisition.md`;
[`../specs/piece-02-relinking.mdx`](../specs/piece-02-relinking.mdx) in full.
**Method:** every attacked number re-measured 2026-08-26 from the committed
corpus (`python` over `extracted/`, buildId 20226581); competitor surfaces
re-probed live with browser-UA curl (+ the one authorized crawler-UA retry).

---

## Attack log

### A1 — "Course definitions carry NO name field (best convention resolves only 12/28)" [priority 1]

The core fact **holds**: no course stub carries any name-shaped carrier
(census of all 69 course rows: only `m_Name` = id, `Description` locstr ×41,
`TannoyNewCourse` str ×26). But the resolution claim is **understated on
both family coverage and arithmetic**, because the sweep stopped at
`Characters/**/<tail>_Qualification_Name`:

- Missed families on disk today: `Courses/DLC_Space/<X>_Name` (7 keys,
  real display names — "Astrology", "Rocket Science", "Cosmic Expansion",
  "Space-Knight School", "Space Academy"…), `Courses/DLC_Ghost/<X>_Name`
  (2 keys — "Paranormal Detection", "School Spirits"),
  `Marketing/Courses/*_Name` (25 keys covering the Marketing_Course_* stubs),
  `Research/Courses/*_Name` (4).
- The report's own cited family `Courses/Courses/<X>_Name` was dismissed as
  wholesale non-mechanical. With light mechanical rules (casefold + plural
  ±s + underscore-suffix tokens), **16/28 courses resolve into the
  `Courses/{Courses,DLC_*}/<X>_Name` families** — including the true display
  names ("Funny Business", "Knight School", "General Knowledge").
- Reproduced report probe under the same rules: qualification-chain direct
  = **14/28** (report: 12); via the hard `Qualification` PPtr → config id
  tail = 12/28; union with the Courses families = **17/28 purely
  mechanically**, and ≈**24/28 with a 7-entry token map**
  (Computing→Computer, Magic→Wizardry, Spy→SpySchool, VeryHard→Hard,
  Grease→Greaser, CheeseAlien→AlienCheese, HumanityAlien→Alien).
  Genuinely nameless in locale data: `Course_SpaceExplorer`,
  `Course_SummerSchool`, `Course_Archaeology_Easter_23` (seasonal),
  `Course_KnightSchool_LordBlaggard` (shares Knight School).
- Also verified: the `Qualification_T_*` configs' own name-ish fields do
  NOT carry per-course names (`ShortDescription` resolves to the shared
  generic "Required to Teach {COURSE}") — the two-hop join through the
  qualification config yields nothing, so the PPtr alone does not solve it.

**Verdict: DOWNGRADED.** G2's conclusion (curated aliases still needed)
survives but shrinks: mechanical floor is 17/28 not 12/28, curated residue
is ~4–7 rows not 28.

### A2 — "GameItemDefinition/Variation lack name fields (2,007 rows)" [priority 2]

Field-level claim verified exactly (class census:
Lite 1,728 all `LocalisedName` · GameItemDefinition 1,654 Description-only ·
Variation 353 neither). The implied hole does **not** survive — display
names arrive mechanically via joins constructible from committed stubs
alone:

1. **Variation → Lite:** every one of the 353 `GameItemVariationDefinition`
   rows carries a hard int ref `GameItem` that resolves **353/353 (100%)**
   to a `GameItemLiteDefinition.DefinitionID`, whose `LocalisedName` is the
   title ("Construction blocks v1", …).
2. **Full → Lite:** **1,652/1,654** `GameItemDefinition` ids have a twin
   Lite def at exactly `<id>_Lite` (name-equality join, piece-02's pinned
   `name-equality(<rule>)` joinKey vocabulary).
3. piece-02 has NOT emitted these as pair rows (`item_item.jsonl` holds
   155 edges, fieldPaths `Item`/`references.*._item` only) — so this is a
   concrete relink-layer work item, not a data hole. Per AGENTS rule 8 it
   is a data-layer task before frontend, sharpening G3's vague routing.

**Verdict: BROKEN (as stated).** Verdict §7.8's "PARTIAL … grouping needed"
is wrong in direction: titles are mechanically reachable for 2,005/2,007
rows today. What remains PARTIAL is only that the relink pair files don't
carry the edges yet.

### A3 — 53.4% searchable universe [priority 3]

The union reproduces exactly **under the report's implicit carrier
definition** (whitelisted carrier fieldPath holding a LocalisedString with
termID≠0 or non-empty `_dev`, plus campus-level plain strings):
carriers 4,298, union 7,178 = 53.39%, per-kind numbers all match
(config 3,856 · item 2,649 · metagame-node 406 · room 107 · student-type 54
· course 41 · unlockable 49 · campus-level 13 · staff 3). But that
definition silently bakes in exclusions the report never ledgers:

- **config plain-string `Name` dev strings ×1,402** (8,430 = 4,559 absent +
  2,421 locstr + 1,450 plain str) are counted as "no field";
- config classes carrying OTHER localized display-name fields:
  `LocalisedName` ×552 (VisitorDefinition 125, CampusEventLiteDefinition 84,
  GameItemUpgradeLiteDefinition 80, RoomLiteDefinition 69…), `Title` ×547
  (CareerGoal*), `DisplayName` ×170 (LandscapeBrushDefinition);
- item titles via the A2 joins (variation+full ≈ 2,005 more);
- room `Name` locstr ×49 beyond the pinned `NameWhenBuilt`;
- unlockable `DisplayName.mTerm` (53 resolving — a literal display-name
  carrier demoted to alias material), `Name` ×44, `DescriptiveName` ×27.

Re-measured expanded universe under these carriers/joins:
**10,200 = 75.9%** (id-only remainder 3,243, not 6,265). Even a conservative
version (report union + item joins + plain-string config Names only) lands
well above 60%.

**Verdict: DOWNGRADED.** The number is internally consistent but must be
re-labeled "direct-carrier floor" — as "the realistic searchable set" it
understates by ~22 points, which would mis-size the search spec (title
plane, kind filters, empty-state design for "no text" kinds).

### A4 — Competitor claims [priority 4] — re-probed live 2026-08-26

| Claim | Result |
|---|---|
| new-world.guide home 706 KB / weapons page 544 KB | ✓ (706,032 / 543,613 B, both HTTP 200) |
| Algolia instantsearch.js **4.48.0** in lazy chunk `2603-…` | ✓ chunk `2603-23dd55c8c80ad584.js` fetched (113,820 B) and contains the literal version string `4.48.0` + instantsearch markers; absent from home critical path (home chunks 282/233/228) |
| i18n keys `search_items`, `search_string`, `searching_items_includes`, `search_perks`, `search_not_found` | ✓ all five present verbatim |
| Single-locale `lang="en-US"`, no hreflang | ✓ (0 hreflang occurrences) |
| wikily.gg `/search` 404 | ✓ (`/search` → 308 → `/search/` → **404**; per-game `/ark-survival-ascended/search/` also **404**) |
| No search library detectable | ✓ (grep algolia/typesense/meilisearch/lunr/minisearch/flexsearch: zero hits in landing HTML) |
| metaforge.app walled 403 ×2 incl. `Claude-User` UA | ✓ reconfirmed this date: browser-UA curl → 403 Cloudflare challenge; single authorized `Claude-User` retry → **403 "Just a moment"**. Stopped there per AGENTS rule 10. G6 wall record is accurate and current. |

**Verdict: SURVIVED.**

### A5 — Shard-size math [priority 5]

- Locale planes: measured 23,107,394 B = 23.1 MB decimal ✓.
- Entity-relevant subset (6,526 keys × 13 locales): raw text 525–693 KB per
  locale (sum 7.59 MB text-only) → with row overhead (~1.24×) the claimed
  **~0.67–0.84 MB/locale** and **9.45 MB raw** both reproduce within a
  percent. SURVIVED.
- **Titles-only "ESTIMATE <150 KB/locale raw": understated.** Measured over
  the actual title-key set (2,993 pinned edges → 2,655 distinct termKeys):
  **163–212 KB raw text per locale (avg 178)** — already above the estimate
  before any JSON overhead, and before the alias block (ids 460 KB +
  `_dev` 239 KB raw) lands. Option C's conclusion survives (title plane
  stays sub-MB), but the number must be corrected or downstream budget
  gates will be set against fiction.

### A6 — Exit-2 tolerance recommendation [priority 6]

All named steady-state numbers match current disk state:
`matrix.json` = 100 cells (**73 missing** / 24 modeled / 3 partial);
`_dangling_guids.jsonl` = **1,137** rows (= `guid_bridge_report.json.danglingDistinctGuids`);
`locale_join_report.json.registryMisses` = **5**;
latest relink run section: `exitCode: 2 (completed-with-ledger)`,
`sourcesApplied=0`, LEDGER-CONTRIBUTORS naming exactly those three ledgers.
The recommendation itself is consistent with piece-02 AC7 semantics
(exit 2 = completed-with-ledger, pipeline continues). **SURVIVED.**

### A7 — Provenance audit + silent scope cuts [priority 7]

Re-derived figures that reproduce EXACTLY: F1 census (13,443 / 9 kinds),
slug 100% null, entity_locale 10,964 rows / 5,850 entities,
locale_term_entity 6,526, registry 15,675 rows / 15,672 keys /
canonical 15,672 / 3 legacy dup rows / locales[] empty on all rows,
§2 carrier table (all nine kind rows), 2,993 edges / 1,895 (kind,title) /
264 collide / Lab Work ×53 / Specialist Book Report ×51 / 320 ignore-kind /
23 markup-only titles (reproduces only when cleaning strips tags AND
`{PLACEHOLDER}` braces), `_dev` fallback 3,874 with exact per-kind split,
mTerm 55/53, Ranks title instances ×30, id tokens 3,224, config classes 344,
Bones[] 139,847, base-overlay 15,665 / 2,824 / 15,402, §3 vocab ×13 exact,
row counts ×13 exact, cross-locale identity rates exact (ru 30.5, ja 16.5,
de 14.3, pt-BR 13.4, fr 10.2, zh/ko ≈1.3), collision counts exact under
distinct-text metric (en 1,570 / zh-Hans 1,668 / ko 1,702 / tr 1,625),
markup 6.0% (any-tag regex).

Corrections found (none load-bearing alone, several compound):

| # | Finding | Effect |
|---|---|---|
| C1 | Registry carries **25 distinct sourceAsset values**, not 26 (I2LS_TEMPLATE dump has 0 terms → no rows). 26 counts dump files. | Minor factual fix in §1/G1 wording |
| C2 | Exactly ONE `mTerms[].Languages` cell in the 26 dumps is non-empty (`SubGoals/Spend_Money_X`, index 14 > mLanguages length 13 → dangling legacy slot, unprojectable). "Genuinely empty in-source" needs this footnote. | Precision note to G1 |
| C3 | unlockable §2 row splits (20≠0/8==0) sum to 28 on field-presence basis; V2-carrier basis gives 20/6=26. Both self-consistent; basis should be stated. | Footnote |
| C4 | ko "CJK-bearing rows 15,213 (98.4%)" conflates Hangul syllables with CJK ideographs under the report's detector (Han-char test finds ~0%). The drawn verdict (ko = whitespace) is right; the column header misleads. | Precision note to §3 |
| C5 | en med/avg lengths measure ~18/51 vs reported 17/47 (methodology difference, direction immaterial); braces-on-en measures 4.5% strict vs reported 4.8%. | Precision note |
| C6 | "room-descriptor 104" icon-sprite count not reproducible (RoomType 52 + RoomTypeBuilding 22 = 74; room stubs 11). Parenthetical optional material — record method or drop. | Unresolved minor |

Silent scope cuts — surface families skipped without gap entries: config
plain-string Names (A3), config extra localized name fields (A3), item
DefinitionID/_Lite joins (A2), DLC/marketing course-name families (A1),
room `Name` locstr ×49, unlockable `Name`/`DescriptiveName`. Each belongs
in the §8 ledger or the §2 tables; together they move the headline numbers
(A1, A2, A3).

## Verdict table

| # | Claim (report) | Verdict |
|---|---|---|
| 1 | Course defs have no name field | SURVIVED |
| 2 | Best convention resolves 12/28 | DOWNGRADED — 17/28 mechanical across three families, ~24/28 with 7-entry token map |
| 3 | Item full/variation defs lack names → grouping needed first | BROKEN — Variation.GameItem→Lite.DefinitionID resolves 353/353; Full↔`_Lite` 1,652/1,654; titles mechanically reachable today |
| 4 | Searchable universe = 7,178 = 53.4% | DOWNGRADED — reproduces only under unstated carrier definition; expanded floor 10,200 = 75.9% |
| 5 | new-world.guide Algolia instantsearch.js 4.48.0 lazy chunk | SURVIVED (live-verified) |
| 6 | wikily.gg /search 404, no search library | SURVIVED (live-verified) |
| 7 | metaforge.app 403 ×2 incl. Claude-User UA | SURVIVED (wall reconfirmed this date; stopped per rule 10) |
| 8 | Sizing 9.45 MB raw / ~0.67–0.84 MB/locale / 23.1 MB planes | SURVIVED |
| 9 | Titles-only <150 KB/locale ESTIMATE | DOWNGRADED — measured 163–212 KB raw text/locale |
| 10 | Exit-2 steady state 73 cells / 1,137 GUIDs / 5 misses / sourcesApplied=0 | SURVIVED (matches current disk) |
| 11 | Registry locales[] empty ⇒ derive from locale bundles (G1) | SURVIVED (footnote C2) |
| 12 | Per-locale shards REQUIRED (§7.3) + rebuild hooks | SURVIVED (consistent with [DR-2026-08-20-locale-urls], piece-02 stamps) |

## Closing verdict

**PASS-WITH-CORRECTIONS.** The report's measurement discipline is strong —
of ~45 re-derived figures, most reproduce exactly, including every number
in its collision/identity/tokenization blocks and the exit-2 ledger. Three
corrections must reach the index spec before it freezes: (1) item titles
are join-derivable today (A2/BROKEN — G3 and §7.8 reframed, and the
Variation/Lite join routed to the relink owners as a concrete work item);
(2) course resolution is 17/28 mechanical with the curated residue down to
~4–7 rows (G2 shrinks); (3) the 53.4% universe figure must be re-labeled a
direct-carrier floor with the excluded carriers ledgered, since the
realistic floor is ~76% (A3). The titles-only payload estimate corrects to
~163–212 KB/locale text-only. No finding invalidates the shard architecture,
the CJK verdicts, the competitor bar, or the exit-2 tolerance recommendation.
