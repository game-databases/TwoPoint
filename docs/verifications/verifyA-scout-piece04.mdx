# verifyA — independent verification of `docs/scout-report-piece-04.mdx`

**Verifier:** verifyA (two-verifier rule) · **Date:** 2026-08-26 · **Input
read in full first:** `docs/scout-report-piece-04.mdx`,
repo-root `AGENTS.md`, `promptForDB.md` (92 lines — contains no dedicated
"evidence-discipline" section; its binding content is the
PrepareTask/pipeline/arbiter rules, so evidence discipline here falls back
to `AGENTS.md` reading rules + extraction-doctrine Principle two:
completeness proven from primary artifacts, not claimed).

**Method:** every load-bearing number re-measured on this machine against
primary artifacts only — `extracted/decompiled/il2cppdumper/{dump.cs,
script.json,stringliteral.json}`, `DummyDll/`, `extracted/decompiled/
structural/`, `extracted/stubs/*.jsonl` (parsed with Python `json.loads`
per line), `extracted/harvest/**` raw monobehaviour JSONs, `extracted/
relinks/{matrix.json,unlockable_unlockable.jsonl}`, `extracted/logic/`.
No binary read into context. Method shown next to each verdict.

---

## Verdict table — fully re-measured claims

| # | Claim (report §) | Verdict | Measured vs claimed + method |
|---|---|---|---|
| 1a | dump.cs ≈42 MB / 20,031 types; TPC 4,752; TPS.Core.GOOSE 126; TPS.Core 110 (§ preamble, §1) | **CONFIRMED** | `os.path.getsize` = 43,935,690 B = 41.9 MiB; count of `// TypeDefIndex:` lines = **20,031** exactly; namespace counters over the same pass: TPC **4,752**, GOOSE **126**, TPS.Core **110** — all four exact |
| 1b | No method bodies anywhere; signatures only (§1) | **CONFIRMED** | Line classification over full dump.cs: **112,282** lines ending `{ }`, **2,302** declaration-only `;`, **0** suspect non-empty bodies |
| 1c | DummyDll 101 assemblies incl. TPS.Game.dll/TPS.Core.dll; script.json 154,493,341 B; stringliteral.json 2,233,838 B; class-hierarchy.jsonl 20,037 rows; id-registries 1,792 files; externals.jsonl 222 rows; logic/ empty (.gitkeep); relink matrix 24 modeled / 3 partial / 73 missing | **CONFIRMED** | All nine numbers reproduced exactly (`ls \| wc -l`, byte sizes, `wc -l`, `json` status counts over `matrix.json.pairs`: {modeled:24, partial:3, missing:73} of 100) |
| 1d | Cited line anchors ~762006 ESActionSatisfyNeedsScore, ~795147 curve Burst delegate, ~839040 StaffBreakUtils PostfixBurstDelegate, ~881087 TotalKudosh, ~816985 EStaffStat, CourseDefinition ~818333 (§1–§3) | **CONFIRMED** | `sed -n` window at each line lands on the named symbol; CourseDefinition is at line **818333** to the digit. `_cfeHasFreeTime`, `_cfeSearchInteraction`, `_cfeActionRoomScore` all fields of ESActionSatisfyNeedsScore |
| 2a | CourseDefinition economics fields serialize (§2) | **CONFIRMED w/ corrections** | Code layer exact: LicenseCost/KudoshCost/YearlyTuitionFee/StartPointsCost all present in dump.cs block @818333. Stub layer: courses.jsonl has **69 rows but only 28 are `TPC.CourseDefinition`** carrying economics; the other **41 are `TPC.MarketingCourseDefinition`** with no Levels/money fields. Report never says "all 69", but §7's "all constants serialized + landed" should be scoped to the 28 |
| 2b | First row Levels[10] PointsCost 20→65 step5; ApplicantsBoost 10→55; Hire 1→10; Train 3→10 (§2) | **CONFIRMED** | Row 1 (Course_Archaeology): PointsCost [20,25,30,35,40,45,50,55,60,65]; ApplicantsBoost [10..55 step5]; HiringTeacherSkillLevelMax 1→10; TrainingTeacherSkillLevelMax 3→10 — verbatim |
| 2c | Spot-check ≥5 courses stub-vs-raw | **CONFIRMED** | 8 courses diffed field-by-field against raw harvest JSONs (`harvest/monobehaviours/configs/TPC.CourseDefinition/configs_assets_all_<pathId>.json`): Archaeology, Archaeology_Easter_23, Clowns, Computing, Gastronomy, Goths, Grease, KnightSchool — **MATCH** on LicenseCost/KudoshCost/YearlyTuitionFee/StartPointsCost + PointsCost ladder |
| 2d | `_assessmentScoring{…}` and `_studentArchetypes[]` serialize per course (§2) | **REFUTED (stub layer) / CONFIRMED (raw layer)** | `_assessmentScoring`: present in **0/69** course stubs; `_studentArchetypes`: **0/69**. In RAW harvest: **28/28** files carry both (e.g. Archaeology raw `_assessmentScoring={BonusPointsPerLevel:10.0, ExpectedAverageXPPerSecond:0.75, PowerFactor:2.0, UseTimeFactors:0}`). The stub emitter dropped these two fields; the report's "first courses.jsonl row carries all of it serialized" is wrong for them |
| 3 | Module chain 319/319 complete: RoomType PPtr, Qualification PPtr, GradeMoneyRewards{AA..F}, ClassSize, Duration, XPMultiplier, GOOSE graphs, Modifiers[], BackupCourseModule (§2, §7) | **CONFIRMED** | 319 `TPC.CourseModuleDefinition` rows; every checked key **319/319** (RoomType, Qualification, ClassSize, Duration, XPMultiplier, GraphStudent, GraphTeacher, GraphSpectator, Modifiers, BackupCourseModule); GradeMoneyRewards keyset = {A,AA,B,BB,C,CC,D,F} ×319. Sample Module_Alien_Science_Year1_Lesson1: ClassSize **8**, Duration **2**, XPMultiplier **1.0** — exact. MINOR: id shape is **312 `Module_*` + 7 `Unused_Module_*`**, so prefix-keyed joins miss 7 rows if strict |
| 4 | Dropout warn 20 / change-mind 15 / urgent 10 / timer 120 s; resignation twin 240 s; StudentFailPercent 10; advisor threshold 20.0 (§2, §4) | **CONFIRMED** | Config_Campus row: StudentDropoutSettings {ThresholdWarning 20.0, ThresholdChangeOfMind 15.0, ThresholdUrgent 10.0, CountdownTimer 120.0}; StaffResignationSettings {20/15/10, CountdownTimer **240.0**}; StudentFailPercent **10**; StudentUnhappyTuitionFeesThreshold **20.0**. Field names match dump.cs structs exactly. Prose literal *"…refuse to pay tuition fees…"* found in stringliteral.json |
| 5a | BudgetType = 27 categories (§3, §7) | **REFUTED → corrected: 28** | Enum body extracted from dump.cs @821639: members TuitionFees=0 … PatientTreatmentIncome=**27** ⇒ **28 named categories**. The report's own inline list contains 28 names while claiming 27; §7 repeats "27 BudgetTypes". Everything else in §3 ledger taxonomy confirmed: ProfitType {TuitionFees,Rent,Bonus}, ExpenseType {Wages,Loans}, Summary columns incl OtherIn/OtherOut |
| 5b | InitialBalance 150000 global vs 80000 Archaeology; fail-states −150000/−300000; 30 FinanceManagerConfig rows (§3) | **CONFIRMED** | Config_FinanceManager {150000, −150000, −300000, RentMult 1.0, TuitionFeesMult 1.0, AllowTuitionFeeModification 1, UseBungleBonus 0}; Archaeology variant differs **only** in InitialBalance 80000 (+m_Name identity); 30 `TPC.FinanceManagerConfig` rows counted |
| 6 | Kudosh separate from money ledger: no BudgetType Kudosh; MetagameLevelState.TotalKudosh carrier; RewardKudoshDefinition._amount; IKudoshUnlockable prices on GameItem+Room definitions; supporting classes/literals (§3) | **CONFIRMED** | Zero Kudosh members among the 28 BudgetTypes; `MetagameLevelState.TotalKudosh` property @~881087 (class @880973); RewardKudoshDefinition {`_amount` int @0x14, `_displayInHUD` bool}; `IKudoshUnlockable` implemented by GameItemDefinition (@824971) and RoomDefinition (@836761); KudoshConsumableRewardConfig = **6** stub rows; GameMod_Kudosh, SubGoal_Earn/Spend/UnlockKudoshItem(+Definitions), RemixChallengeOption_Kudosh all exist in dump.cs; literals KudoshEarn:UI / KudoshSpend:UI present |
| 7a | Research: 450 project rows = 209 + 241 (+4 containers); named costs Bookshelf_Robotics 250, Computer_Digital 500, Computer_Super 500, Course_Computer 1200 (§3, §7) | **CONFIRMED** | metagame-nodes classes: ResearchProjectDefinition **209**, Lite **241**, Database ×2 + LiteDatabase ×2 (=454 total ✓). Named costs measured **250/500/500/1200** under ids `ResearchProject_*` (bare names don't exist as ids). EStaffStat ResearchPoints=8, ResearchPointsThisYear=9 ✓ |
| 7b | Cost domain {250, 500, 1200}; "450 project rows w/ costs landed" (§7 basis) | **REFUTED → corrected** | Full domain across the 209 defs: **{100×4, 200×1, 250×12, 300×52, 500×82, 600×27, 1200×29, 2500×1, 3000×1}** — wider than the three sampled values; and costs ride on **209/450** rows (Lite defs carry none). "450 rows w/ costs landed" overstates coverage |
| 7c | EStaffStat = "full 23-stat enum" (§3) | **REFUTED → corrected: 31** | Enum body @816985: None=0 … GhostsCaptured=**30** ⇒ **31 members** |
| 8a | Config_Campus teaching 0.6 base, multipliers 0.75–1.25, DesiredSalaryIncrease 0.30; XP boosts 2.0/2.0 (§4) | **CONFIRMED** | Config_Campus stub: TeachingBaseRate 0.6, TeachingMinMultiplier 0.75, TeachingMaxMultiplier 1.25, DesiredSalaryIncrease 0.30; StudentXPMultiplierExtraTuition 2.0, SelfStudy 2.0; Teacher/StudentEnergyModifierDuringClass −0.1 each |
| 8b | RoomPrestigeLevels[15]; L1 5 pts/×1.0 happiness; L5 45 pts/×8/boosts .1/staff energy ×1.75/dormitory .85 (§4, §7) | **CONFIRMED** | Array length **15**; L1 {Points 5, HappinessModifier 1.0, DormitoryPrestigeFactor 0.5}; L5 {Points 45, Happiness 8.0, boosts 0.1, StaffRoomEnergyMultiplier 1.75, DormitoryPrestigeFactor 0.85} — exact |
| 8c | Remaining Config_Campus groups: satisfaction curves/clubs(.5,10)/relationship(2,20)/dorm 2.5/band ±0.25; staff SalaryCurve .75–1.25, CareerSmoothTimeInYears 3; hygiene 25/−0.5(+75/−1.0); intake caps 150/200/300; ability 0.5/1.5; personal goals 90/180/5; HUD thresholds .25/.25/80/90/warning 20; ItemPowerDiminishingReturnsCurve {_type:0,_minX:1,_maxX:10,_maxY:0.1}; RoomCustomisationPrestige 1.0 (§4) | **CONFIRMED** | Every value reproduced from the Config_Campus stub row (84 top-level keys); FM base-vs-Archaeology diff = InitialBalance only |
| 8d | item→environment via RoomModifier_EnvironmentAttribute{_attribute,_value,_radiusInCells,_startApplyingLevel,_maintenanceMultiplier} (§4) | **CONFIRMED w/ coverage note** | Code fields exact (@dump.cs); typed data blocks present e.g. Item_Cafeteria_Prop_Burger {_attribute:1, _radiusInCells:2.0, _maintenanceMultiplier:0.0,…}. Coverage is thin though: **6 items.jsonl + 8 configs.jsonl** rows carry `_radiusInCells`, and raw layer agrees (6/1698 GameItemDefinitions) — so the mechanism is data-driven, the per-item map sparse, not absent |
| 9a | Prerequisite taxonomy: abstract Prerequisite + 24 concrete subclasses (§2) | **CONFIRMED** | Regex over full dump.cs finds **exactly 24** `class X : Prerequisite` subclasses, mapping 1:1 onto the report's name list (DaysPassed…ChallengeAcademicScore) |
| 9b | Census: 977 `Prerequisites` occurrences; HasCourseUnlocked ×50, UniversityLevel ×43, Stars ×26, HasRoomUnlocked ×18, DaysPassed ×17 (§2) | **CONFIRMED (method note)** | Typed-class census over configs.jsonl SerializeReference blocks: HasCourseUnlocked **50**, UniversityLevel **43**, LevelPrerequisiteStars **26**, ChallengePrerequisiteHasRoomUnlocked **18**, DaysPassed **17** — all five exact. "977 Prerequisites field occurrences" reproduces under **substring** counting (`grep -o 'Prerequisites' \| wc -l` = 977); exact `"Prerequisites"` key = 675 (+68 VisiblePrerequisites +68 StartPrerequisites). Number correct, counting basis unstated |
| 9c | Payload shape on CareerChallenge_Course_Archaeology_V1; cross-file PPtr m_FileID 2 edges blocked on externals resolver (§2, §6, G2) | **CONFIRMED** | Stub carries `"data":{…,"_visibleOnHUD":0},"type":{"asm":"TPS.Game","class":"PrerequisiteHasCourseUnlocked","ns":"TPC"}` verbatim; externals.jsonl exists (222 rows) and no pathId-level resolver exists in relinks (matrix mechanisms: hard/inferred only) |

## Sampled claims (≥3 per remaining section)

**§5 needs/AI**
- EAttribute 11 members Energy=0…Fun=10 — **exact**; ECCharacterAttributes `Max=11`, static `Needs[]`, one AttributeValue each — **confirmed**.
- CharacterAttributeStatusEffectsDefinition full field list incl. nested AttributeEffects/PeriodicalAttributeEffect shapes — **confirmed** verbatim.
- GraphDefinition {_versionHash, Name, GraphType, StartStateID, States, Variables[IGraphVar], Transitions} — **confirmed**; and topology really is data: **639** `TPS.Core.GOOSE.GraphDefinition` stub rows, all carrying States.
- ActionSatisfyNeeds._aiGraph + SaveData{Score, RoomID}; ESActionSatisfyNeedsStart/Update/End/Score all exist — **confirmed**.
- CampusEventDefinition, Timetable, CharacterTraitDefinition, StaffTrainingDefinition.XPRatePerSecond, StudentDefinition XP fields — **all confirmed** (NumPeriodsPerYear string does exist in dump.cs).
- **REFUTED-IN-PART:** "No config payload was found carrying base decay rates — ChangeOverTime is runtime-set" (§5) / G3 "zero carriers found". Serialized carriers DO exist: `StudentDefinition` stubs embed a typed `ECPStudent` component block with three `AttributeDefinition`s — `_clubNeed` ChangeOverTime **−0.1/s** (band 80–100), `_relationship` **−0.2/s** (40–100), `_selfStudy` **−0.2/s** (40–100) — where `AttributeDefinition = {Disabled, MinInitialValue, MaxInitialValue, ChangeOverTime}` (struct @dump.cs TypeDefIndex 13604); plus `ClubDefinition.ClubNeedChangeOverTime` (−0.04 etc.) in 7 club rows. What stays genuinely data-absent: change-over-time for the core survival attributes (Energy/Happiness/Hygiene/Food/Drink/ToiletComfort/Litter/Social/Health/Sober/Fun). Consequence: feasibility row "Needs decay simulation — Impossible-from-corpus-as-is" must downgrade to **partial** (3 non-core needs already reconstructable).

**§6 join-key contract**
- Key ids verified present verbatim: `Config_Campus`, `Course_*` (69), `ResearchProject_*`, `CareerChallenge_*`; typed SerializeReference blocks inline `type.{asm,class,ns}` demonstrated repeatedly — **confirmed**.
- MINOR correction: "career challenges `CareerChallenge_Course_*` (67 rows)" conflates two counts — **67** = all `TPC.CareerChallengeDefinition` rows; ids starting `CareerChallenge_Course_` = **17**.
- MINOR: `Module_*` prefix covers 312/319 (see #3).
- GUID→catalog 79.7% resolve rate and the 23,250 cross-file refs are piece-02 citations, not re-derived here (carried-over, flagged as such in the report itself).

**§7 feasibility matrix**
- Course licensing/tuition "Full reconstruction": holds for the **28** real CourseDefinitions + CoursesConfig nested overrides ({InitiallyAvailable, DefaultIntake, IntakeMultiplier, StartPointsMultiplier, UpgradePointsMultiplier, OverrideDefaultLicensing, LicenseCost, KudoshCost, Objective} — all measured on Config_Courses' Courses[] entries; 36 CoursesConfig rows).
- Module→room chain "Full reconstruction": holds (319/319, see #3).
- Money ledger "Full taxonomy": holds modulo the 27→28 fix.
- Needs decay "Impossible-from-corpus-as-is": **downgraded to partial** per the G3 finding above.
- Timetable "Partial (solver native)": consistent — TimetableScheduler exists in dump.cs, event params (CampusEventDefinition) fully data-side.

**§8 G-gap ledger spot-checks**
- G2 unblock artifact present: harvest/externals.jsonl 222 rows ✓.
- G7: stub side exact (**48 rows × 4 generic fields** m_Enabled/m_GameObject/m_Name/m_Script); raw side `_decoded.typetreeDecoded: true` with PlotRecords **[7]** (BuildCost 100, 1000-series values + BuildDurationDays **10.0**) and RoomRecords **[21]** — substance confirmed; one nit: that sample file measures **1,287,597 B (~1.26 MB)**, not "248 KB".
- G8/Burst framing consistent with §1 measurements (PostfixBurstDelegate types present at cited lines).

---

## Corrections list

1. **BudgetType = 28 categories, not 27** (§3 bullet + §7 row). Enum spans TuitionFees=0 … PatientTreatmentIncome=27.
2. **EStaffStat = 31 members, not "23-stat"** (§3 research bullet).
3. **G3/§5 needs-decay claim overstated**: ChangeOverTime IS serialized for club-need/relationship/self-study (ECPStudent blocks in StudentDefinition stubs; ClubNeedChangeOverTime in ClubDefinition). Only the core 11-attribute decay rates remain corpus-absent; feasibility row becomes *partial*, not *impossible*.
4. **courses.jsonl composition**: 28 TPC.CourseDefinition (full economics) + 41 TPC.MarketingCourseDefinition; and the stub emitter dropped `_assessmentScoring` + `_studentArchetypes` (present 28/28 in raw harvest JSONs) — ingest point for those two is the raw layer, or fix the emitter.
5. **CalculateRoomScore = 7 static overloads, not ≥8** (dump.cs lines 755690/756166/758135/758614/759951/760527/762917); variants cover medical-care, medical-consultation, pastoral-care, patient-treatment, idle, staff-break — **no extra-tuition variant** by that name (§4 "formulas not data-driven" item 2).
6. **Module id shape**: 312 `Module_*` + 7 `Unused_Module_*` (count 319 correct); prefix-keyed joins must include `Unused_`.
7. **CareerChallenge counts**: 67 = all CareerChallengeDefinition rows; `CareerChallenge_Course_*` prefix = 17 (§2 join keys).
8. **Research cost domain** is {100,200,250,300,500,600,1200,2500,3000} over 209 cost-bearing defs (Lite defs carry none) — "450 rows w/ costs landed" should read 209/450 (§7 basis row).
9. Nit: G7 sample file size ~1.26 MB, not 248 KB; "977 Prerequisites occurrences" is substring-counted (exact-key count 675).

None of these overturn any §7 verdict except the needs-decay row, which improves.

## Overall verdict

**PASS-WITH-CORRECTIONS** — every headline count and constant in scout-report-piece-04 reproduces from primary artifacts (substrate sizes/types, 319/319 module chain, dropout/resignation thresholds, finance configs, Kudosh separation, research row counts and named costs, full Config_Campus value set, 24-prerequisite taxonomy, typed-block counts 3,330/1,787/130), subject to the nine corrections above, of which #3 (needs-decay carriers exist) and #4 (stub-layer assessment-scoring/archetype drop) are the only ones that change a downstream conclusion.

END OF docs/verifications/verifyA-scout-piece04.mdx
