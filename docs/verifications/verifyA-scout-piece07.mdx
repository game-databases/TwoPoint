# verifyA — independent verification of `docs/scout-report-piece-07-locales.mdx`

**Verifier:** verifyA (two-verifier rule) · **Date:** 2026-08-26 · **Input
read in full first:** `docs/scout-report-piece-07-locales.mdx` (485 lines,
through `§END`), repo-root `AGENTS.md`, `promptForDB.md` (92 lines — no
dedicated evidence-discipline section; binding content is the
PrepareTask/pipeline rules, so evidence discipline falls back to
`AGENTS.md` reading rules + extraction-doctrine Principle two:
completeness is proven from primary artifacts, not claimed).

**Method:** every load-bearing number re-measured on this machine against
primary artifacts only — `extracted/bundle-roster.jsonl`,
`extracted/identity.json`, `extracted/locales/*.jsonl` (14 files),
`extracted/locales/base-overlay-report.json`,
`extracted/locales/locale-matrix.json`, `extracted/stubs/*.jsonl` (9 kind
files, parsed per-line with Python `json.loads`),
`extracted/relinks/{entity_locale,i2_term_registry,locale_term_entity}.jsonl`,
`extracted/relinks/{locale_join_report.json,locale_availability.jsonl}`,
and `_foundation/localization-architecture.md` §5 read directly for the
§5.4 half of claim 6. No binary read into context. Scratch measurement
files were removed after use; nothing else in the pack was touched and
nothing was committed.

---

## Verdict table — the eight fully re-measured claims

| # | Claim (report §) | Verdict | Measured vs claimed + method |
|---|---|---|---|
| 1 | Roster: exactly 13 text locales + 1 unnamed base overlay = 14 locale bundles; total payload 6.54 MB; all `dirClass: base` (§1) | **CONFIRMED** | Count over `bundle-roster.jsonl` rows with `localeFlag != null`: **14** of 176 rows; summed `bytes` = **6,541,486 B** — matches the report to the byte. Per-bundle sizes match the §1 table cell-for-cell (base 252,920 … ru 533,769). All 14 carry `dirClass:"base"`; flags = {base, en, pt-BR, zh-Hans, zh-Hant, fr, de, it, ja, ko, pl, ru, es, tr} = 13 named + base. Cross-check: `identity.json.localeBundleCount` = **14**, buildId 20226581, version string matches preamble |
| 2 | Key plane: union of 13 tables = 15,666 distinct keys; 15,369 (98.11%) present in all 13 locales (§2.1) | **CONFIRMED** (one rounding slip) | Independent set-union over the 13 emitted tables: **15,666** distinct keys; all-13 intersection **15,369**. Full presence histogram reproduced exactly: 13→15,369, 12→46, 11→27, 10→1, 9→2, 3→1, 2→15, 1→205. Per-locale rows/missing exact: en 15,665/1, ko 15,457/209, zh-Hant 15,446/220, zh-Hans+de+fr+it+pl 15,445/221 each, pt-BR/tr 15,443/223, es 15,440/226, ru 15,422/244, ja 15,371/**295** (worst). MINOR: 15,369/15,666 computes to **98.10%**, not the printed 98.11% |
| 3 | Entity plane: joined entities 98–100% coverage in every locale; items any/full 100% everywhere except ja 99.2% and ru 99.2% (§2.2) | **CONFIRMED** | Recomputed all 13 locales × kinds from `entity_locale.jsonl` term sets joined to the 13 tables: item (2,035 ents) any/full = **99.2% ja, 99.2% ru** (tie), 100.0% in the other 11; config (3,178) any 98.8–99.0 / full 97.8–98.1 (report's "99.0/98.0" holds to rounding); room (106), student-type (54) 100/100; metagame-node (390) 99.0; unlockable (43) 100; course (41) 100. Identical-to-en name rates reproduce EXACTLY under the reconstructable rule "edge whose `evidence.fieldPath` contains 'name', first such edge per entity": item 1,077 name-ents, ja **10.96%** / ru **23.03%**; room 105, ru **19.05%**; metagame-node 195, ru **28.21%**; unlockable 23, ja **86.96%** / ru **91.30%**; student-type 27, ru **37.04%**. Config only approximates under any tie-break tried (denominator 2,613 vs implied 2,627; ru 30.50% vs claimed 30.95%; ja 18.45% vs 18.39%) — but its zh minimum reproduces exactly (**7.35%**), so this is an unstated tie-break ambiguity, not a misread artifact |
| 4a | Join gap: 10,964 edges over 5,850 entities; 5,850 of 13,443 stub rows have any term join (§2.2, §3, §5) | **CONFIRMED** | Stub row counts per kind sum to **13,443** with the exact per-kind map {config 8430, item 3885, room 116, metagame-node 454, unlockable 415, course 69, student-type 54, staff 3, campus-level 17}. `entity_locale.jsonl` has **10,964** rows (= `locale_join_report.registryHits`, perKindHits sums to it too); distinct `(srcKind,srcId)` pairs = **5,850** (also 5,850 id-distinct — no cross-kind collisions); per-kind joined map matches the report's table cell-for-cell incl. campus-level **0** |
| 4b | Courses have ZERO name-role terms (only descriptions); campus-levels 0/17 joins; unlockables ~10% joined (§2.2) | **CONFIRMED** | All 41 course edges carry `fieldPath:"Description"` and no course edge targets a `*_Name` key → zero name-role terms, as claimed. Campus-levels: 17 stub rows, 0 entity_locale edges. Unlockables: 43/415 = **10.36%** ≈ "~10%" |
| 5 | Base overlay: ALL 15,672 rows empty-text — pure master registry (§1, §4.1, G7) | **CONFIRMED** | Walked `locales/base-overlay.jsonl`: **15,672** rows; rows with non-empty `text`: **0**; emptiness rate **100.00%**. Consistent with `base-overlay-report.json` (`baseRowCount` 15672, `baseCellsSkippedEmpty` 15677, `identicalTextSharedKeys` 0) |
| 6 | `relinks/locale_availability.jsonl` is empty (0 rows) AND localization-architecture §5.4 names that file the single source for link rows/sitemaps/coverage stats (§5 G1) | **CONFIRMED (both halves)** | File size **0 bytes**, **0 lines**. `_foundation/localization-architecture.md` §5.4 verbatim: "*`extracted/relinks/locale_availability.jsonl`* — one row per entity … This file is **the single source for the §2 link row, per-locale sitemaps, and coverage stats.** Regenerate on every extraction rerun." Both halves stand; the gap is real |
| 7 | ru carries 297 `Items/*_Name` values byte-identical to English (§2.1 quirk, G6) | **REFUTED → corrected: 301** | Plain recount over the 905 `Items/*_Name` keys: **301** keys where ru text == en text (non-empty; also 301 distinct values, so not a distinct-value count). No natural filter yields 297 except adding an unstated extra condition ("de differs" gives exactly 297), which the report does not state. The adjacent claim IS confirmed: of the 905 keys, **zero** are identical-to-en in *every* locale, and the worked example verifies (`Items/Library/Reception_Giant_Name` en=ru="Giant Library Reception", de="Riesige Bibliotheksrezeption"). Correction is −4 on one derived stat; direction and magnitude of the finding (ru under-translates item names, ja/ru worst) unchanged |
| 8 | Holes concentrate in `Challenge/*` + `Levels/*` event strings (~115–119 Challenge per locale; Levels 32) (§2.1, G4) | **CONFIRMED** | Namespace histogram of missing keys recomputed per locale: `Challenge/*` = **115** holes in each of ru/es/ko/de (and 119 in ja — inside the stated 115–119 band); `Levels/*` = **32 in every** non-en locale. Next namespace trails far behind (Meta 44 in ja, 16–21 elsewhere). The 205 single-locale keys decompose as Challenge 115 + Levels 32 + Advisor 17 + Objectives 13 + Characters 8 + rest — "seasonal/challenge narrative text" is accurate |

## Secondary spot-checks (load-bearing neighbors, cheap to re-measure)

| Claim (report §) | Verdict | Measured |
|---|---|---|
| Registry 15,675 rows / 15,672 distinct keys; TermStatus 15,400 ForTranslation / 275 NotForTranslation; termType uniform 0; `locales[]` empty on every row (§1, §3) | **CONFIRMED** | All five exact over `i2_term_registry.jsonl`; status enum values {1:15400, 0:275}; `matrixKeyDiff = 0` re-derived against `locale-matrix.json` (15,672 ↔ 15,672, both directions empty; `includesBaseKeys: true`) |
| Usage projection: 6,526 referenced keys; orphans direction B = 0; orphans direction A = 9,148 rows / 9,146 keys (§3) | **CONFIRMED** (one ns delta) | `locale_term_entity.jsonl` = 6,526 rows = 6,526 distinct used keys; used∖registry = 0; registry-unused = 9,146 keys over 9,148 rows. Namespace histogram matches except UI measures **1,686** vs claimed 1,688 (same ±2 drift as the §5 census below); ArchetypeNames 1535, Names 908, VO 901, Levels 773, SubGoals 516, Challenge 477, FlavourTraits 400 all exact |
| Base-only keys vs en = the listed 7 (§4.2) | **CONFIRMED** | Exactly those 7 keys, same list |
| English-only kernels: unlockable 372 no-join / 41 carry non-empty EN literals / 3 coincide with some EN term text; item 1,850 no-join with kernels Item_Editor\* 713, Unused_Item\* 231, Item_LS\* 141, A_LS_Variation\* 134, Variation_\* 219; config 5,252 no-join (§2.2) | **CONFIRMED** | Every figure exact (372/41/3; 1850 + 713/231/141/134/219; 5252). Sample literals verified ("Idle", "Cupid Relationship") |
| Campus-levels: "all 17 rows carry plain English `Name` literals" with samples "All buildings", "Blank Level", "Test Level", "Mark's Test Scenario" (§2.2 #1) | **CONFIRMED w/ correction** | 17 rows, 0 joins ✓; all four quoted samples exist verbatim. But only **13/17** rows carry a display `Name` literal — 4 rows (`Config_UniversityLevel`, `Config_UniversityLevel_Puzzle_Remix`, `Config_UniversityLevel_ZeroMoney_Remix`, `LevelScenarioV2_FreePlay_City`) have no `Name` field value at all (internal `m_Name` identifier only). "All 17" overstates by 4; V5's substance (genuinely english-only kernel) unaffected |
| §5 game-UI namespace census: UI 2,214 keys (533 referenced / 1,688 free); Input 164; General 143; 52 top-level namespaces | **CONFIRMED w/ correction** | Input **164** ✓, General **143** ✓, top-level namespaces **52** ✓. UI measures **2,218** distinct registry keys = **532** referenced + **1,686** unreferenced; note the report's own three numbers do not even sum (533+1688=2221≠2214). Cosmetic drift, feeds nothing downstream |
| Meta/CareerGoals en+ko cluster of 15 (§2.1); en misses only `UI/General/NameSeparator`, held by zh-Hans/zh-Hant/ja (§2.1, G5) | **CONFIRMED** | Exactly 15 `Meta/CareerGoals/*` keys held only by {en,ko} (of 116 total in that family); en's single missing key is `UI/General/NameSeparator`, held by exactly {zh-Hans, zh-Hant, ja} |

## Closing

Corrections carried (none change any feasibility verdict V1–V7, gap
G1–G8, or the piece's route):

1. **G6/§2.1**: ru identical-to-en `Items/*_Name` count is **301**, not
   297 (297 requires an unstated "de differs" filter).
2. **§2.1**: all-13 key share prints 98.11%; correct value is **98.10%**
   (15,369/15,666).
3. **§2.2 kernel #1**: "all 17 campus-level rows carry plain English
   Name literals" → **13 of 17**; four rows have no `Name` field.
4. **§5**: UI namespace census should read **2,218 / 532 referenced /
   1,686 free** (report's own figures are mutually inconsistent);
   **§3**: orphan-direction-A UI count is **1,686**, not 1,688.

**VERDICT: PASS-WITH-CORRECTIONS** — corrections 1–4 above; every other
measured claim in the eight target areas reproduces from primary
artifacts, most to the exact digit.
