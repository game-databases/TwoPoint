# CodeReview codereviewer-002 — fix-round verification on piece-1

**Reviewer:** codereviewer-002 (independent; spawned no subagents;
verified only — fixed nothing). **Date:** 2026-08-24.
**Inputs read in full:** [`../rulings/arbiter-002-piece-01-build.mdx`](../rulings/arbiter-002-piece-01-build.mdx),
[`../reviews/codereviewer-001-piece-01.mdx`](../reviews/codereviewer-001-piece-01.mdx),
`run_all.py`, `tools/*.py` (all 13 files), the 11 fix commits
(`717c126…3d4b99b`) diff-read individually, spec §3/§4 incl. Revision 3.

**Method:** every F-item was verified against source AND, where the
mission allows, against live execution: stage 0 re-run read-only against
the real client at `A:\SteamLibrary\steamapps\common\Two Point Campus`
through a junctioned throwaway tree (client untouched; temp trees
deleted after); a second corrupted-suffix tree proves the self-check
fires; stage 5 exercised hostless for the absence-ledger legs;
`resolve_tool`, the stamp layer, `--list`, and the settings-snapshot
parse driven in-process against real files. Hostless suite re-run:
**79 passed / 6 skipped / 0 failed** (matches the independently
confirmed state). Stages 1+ were NOT run against the real client.

## Verdict per fix item

| Fix | Verdict | Decisive evidence |
|---|---|---|
| F1 | APPLIED-CORRECTLY | `tpc_common.py:313`; self-check `stage0_verify_client.py:150-169`; both live legs below |
| F2 | APPLIED-CORRECTLY | `stage5_emit_stubs.py:258-261`; domain = hard-joined only |
| F3 | APPLIED-CORRECTLY | `tpc_common.py:108-113` + `stage1_decompile.py:40-42`; both resolution legs live-land in-repo |
| F4 | APPLIED-CORRECTLY | `stage3_harvest_bundles.py:250` (`Counter`); message/exit identical |
| F5 | APPLIED-CORRECTLY | index built once in load pass (`stage5_emit_stubs.py:317-324`), join from memory `:224-228` |
| F6 | APPLIED-CORRECTLY | scope written before id gate `:329-335`; live-verified non-empty |
| F7 | APPLIED-CORRECTLY (sanctioned alternative) | case-fold lookup `stage2_harvest_catalog.py:198-202`; live-verified vs real settings.json |
| F8 | APPLIED-CORRECTLY | `run_all.py:176-213`; live `--list` shows version column |
| F9 | APPLIED-CORRECTLY | loader reconstructs `.bundle` basename `stage5_emit_stubs.py:141-144` |
| F10 | APPLIED-CORRECTLY | evidence split by cause `:338-340`; one INFO nuance below |
| F11 | APPLIED-CORRECTLY | helpers verbatim, pinned signatures + `(ok, problems)` shape; messages byte-identical |
| F12 | APPLIED-CORRECTLY | `build_locale_matrix(tables)` single-arg; run() keeps meta+write; output byte-identical |

Fix rounds 2–3 (post-ruling defects A/B):

- **Stamp layer (F13) — APPLIED-CORRECTLY.** `STAGE_OUTPUTS` covers ALL
  six stages (`log_util.py:201-221`), localisation's 13 per-locale
  tables appended from the shared table (`:256-264`). Live-exercised:
  exit-0 stamps fingerprint outputs; deleting `bundle-roster.jsonl` or
  rewriting `identity.json` flips `is_up_to_date` → False (re-execute);
  pre-fingerprint stamps count stale once; an existing empty file and an
  absent file hash differently so legitimately-empty ledgers never cause
  perpetual reruns; `--list` status reflects currency
  (`run_all.py:170-172`). Byte-identity exclusions untouched —
  EXTRACTION-LOG.md / .stage-stamps/ / .pipeline-meta.json deliberately
  unlisted.
- **Absence ledger (F14) — APPLIED-CORRECTLY.** `_absences.jsonl` rows
  carry `buildId` from identity.json (== TargetBuildID 20226581 on the
  real client; no drift line fired). Zero-candidate route live-verified:
  with only one item dump present, all 8 rowless kinds report the TRUE
  scan universe (`scannedBundles:['x.bundle']`,
  `scannedClasses:['ItemConfig']`), never empty lists. Zero-row kinds
  emit NO empty `<kind>.jsonl` final — matching spec §3's pinned
  "non-empty `<kind>.jsonl` XOR `_absences.jsonl` row" acceptance.

## Mission confirmations

### F1 — both halves, live on the real client

- **Legit leg:** junction tree over the A:\ install → stage 0 exit 0,
  176 roster rows, 14 locale-flagged (1 base overlay +
  `localisation_assets_localisation.bundle`), and the 13 named flags are
  EXACTLY `sorted(EMITTED_LOCALES)` = de en es fr it ja ko pl pt-BR ru
  tr zh-Hans zh-Hant. Zero `unknown:*` flags.
- **Corrupt leg:** same tree plus a copy of the english bundle under the
  name `localisation_assets_localisationklingon.bundle` → **exit 1**
  with `offending rows: ["…/localisation_assets_localisationklingon.bundle -> 'unknown:klingon'"]`
  and the unresolved-locales list — the self-check fires and names rows,
  as ruled.
- The verbatim `unknown:<suffix>` surfacing is preserved
  (`tpc_common.py:315-318`); `lstrip("_")` resolves both separator
  generations (see observation O-A).

### F3 — both halves

Constants repo-relative, order exactly as pinned (zero-parades staged →
disco-elysium/zero-parades → disco-elysium/tools → `D:\` absolute);
both in-repo copies verified existing, `disco-elysium/zero-parades`
absent everywhere as ruled. `resolve_tool` appends embedded candidates
after the log-derived list with first-occurrence dedup: live-driven with
a seeded stale log naming two dead paths → resolves to
`game-databases\zero-parades\work\_tooling\il2cppdumper\Il2CppDumper.exe`;
fresh-log leg lands identically. A stale seeded block can no longer
shadow the corrected entries.

### F5 — semantics and hot path

Join is `dumps_source.get(pathId, [])` over the index built during the
single load pass; winner still requires the identifier byte-match
(`extract_id(fields) == eid or extract_id(payload) == eid`) — id-match
semantics preserved. No rglob remains inside any per-entity loop; the
only remaining tree walks are the single load pass
(`:127`) and the one-pass acceptance check (`:419`).
Directory argument still accepted via `index_monobehaviour_dumps`
(direct-caller path, not dead code); unused `_catalog` load dropped per
the ruling's sanctioned rider.

### Regressions — none found

- **Determinism discipline:** atomic temp+`os.replace`, sorted-keys
  JSON, UTF-8/LF writes all unchanged; every fix diff confined to ruled
  lines plus comments (diff-read commit by commit).
- **Exit-code contract:** stage 3's exit-2 ledger path unchanged;
  runner's break-on-1/3, continue-on-2 mapping unchanged; refusal stamps
  save exit codes without fingerprints (correctly excluded from
  up-to-date since `exitCode != 0`).
- **Carve-out:** `EXPORT_CLASSES`/`CARVE_OUT_CLASSES` untouched;
  catalogue-only routing intact.
- **Sole ownership of relinks:** stage 4 writes nothing under relinks/
  (log line says so explicitly); stage 5 rmtree+regenerates
  `locale_availability.jsonl` every run.
- **Suite:** 79 passed / 6 skipped / 0 failed — identical to the
  confirmed baseline; the one banner note ("coverage function present
  but signature differs") is T5's banner accounting working as designed,
  TEST-lane vocabulary, not impl drift.
- No new dead code or behavior drift beyond the ruled items found.

## Observations (INFO — no action required)

- **O-A: D1's real-client premise was imprecise; the fix is right
  anyway.** The actual install names carry NO separator
  (`localisation_assets_localisationenglish.bundle` — scout-report-001's
  table :258-274 records this correctly), so the pre-F1 lookup would
  already have matched real names; codereviewer-001 D1's quoted
  `…_english.bundle` spellings misquote that table and match only
  fixture-generation names. Landed F1 handles BOTH spellings
  (`locale_for_bundle` live-verified on each) and the stage-0 self-check
  makes flag corruption structurally unpassable regardless of naming
  generation. Recorded so future sessions trust the roster, not the
  review prose.
- **O-B:** `_unmapped-families.jsonl` evidence sticks per class
  (`setdefault`): a class whose objects split across both no-kind /
  kind-but-no-id routes carries whichever cause string was created
  first. Each string remains accurate for ≥1 object in the bucket; spec
  requires class-granular rows only.
- **O-C:** `STAGE_OUTPUTS` declares finals/ledgers but not bulk planes
  (`harvest/textassets/**`, `harvest/monobehaviours/**`,
  `census/bundles/**`, `decompiled/il2cppdumper/**`). Deleting a bulk
  plane leaves its stage "up-to-date" — monobehaviours deletion is still
  caught downstream (stage-5 upstream identity hashes the file-name
  list); textassets has no piece-1 consumer. Consistent with the
  "declared final artifacts" scope; fingerprinting tens of thousands of
  files per check would be the alternative and is worse.
- **O-D:** `--list` now hashes declared outputs per stage
  (`outputs_current`); negligible at current scale, noted for
  completeness.

## Verified-good summary (evidence)

Stage 0 twice against the real client through junction trees (legit:
exit 0, exact 13 BCP-47 flags, identity complete with addressables
1.21.10 + settingsHash; corrupt: exit 1 naming the offending row) — temp
trees deleted afterward, client untouched. Stage 5 hostless: rc 0,
absence rows carry buildId + true scan universe, no empty kind finals.
`resolve_tool` both legs land in-repo. Stamp layer delete/rewrite/
pre-fingerprint/empty-ledger legs behave as specified. `--list` shows
the pinned version column (`1.25.3` for UnityPy, `-` unstamped dumper).
Settings snapshot parse resolves all four pinned keys from the real
`m_buildTarget`-spelled file to canonical spellings
(m_BuildTarget=StandaloneWindows64).

END OF codereviewer-002-fixrounds
