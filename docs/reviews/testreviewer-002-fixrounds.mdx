# TestReview 002 — piece-01 suite, fix-round verification

**Reviewer:** TestReviewer-002 (independent; judges, does not fix; spawned
no subagents). **Date:** 2026-08-24.
**Subject:** the TEST-lane fix rounds against
[`docs/rulings/arbiter-002-piece-01-build.mdx`](../rulings/arbiter-002-piece-01-build.mdx)
(T1–T7), measured against the baseline
[`testreviewer-001-piece-01.mdx`](../testreviewer-001-piece-01.mdx)
(coverage matrix + D0–D8).
**Inputs read in full:** the ruling, reviewer-001, every file under
`tests/` as they now stand, and the six T-lane commits diff-read hunk by
hunk (`git log --oneline -15 -- tests`): `c08e366` (T1), `84897a6` (T2),
`7d2020f` (T3+T5), `8892e15` (T4), `a2013fe` (T6), `d93588d` (tests
repair: missing `JOIN_NAMES` import in `test_stage5.py`, same latent-
NameError class as T1, found at verification).

---

## Verdict: FIX-REQUIRED (one residual, outside the T-lane; details below)

Every T-item landed as pinned, the four formerly loud-skipped obligations
now EXECUTE green against the extracted seams, and the suite is at the
ruling's exact success signal:

```
python -m pytest tests/ -q   (this host, NE8K, Python 3.14.7 + pytest 9.1.1)
79 passed, 6 skipped in ~19s      — 0 failed
skips: 5 × client-gated-heavy (TPC_IT_HEAVY=1 opt-in) + 1 × `make` not on PATH
       — every one loud, reason-named in the short summary
85 collected (+1 vs reviewer-001's 84 = T4's new test); pytest.ini carries
no suppression flags (-ra --strict-markers only)
```

The one FIX-REQUIRED residual (**R1**) is a *pre-existing* heavy-gated
assertion that Revision 3 (adopted this round per S1) silently invalidated;
it is not a regression introduced by any fix commit. Nothing in T1–T7
warrants reopening.

## T-item verdicts

| Item | Verdict | Evidence |
|---|---|---|
| T1 | **APPLIED-CORRECTLY** | `c08e366`: `from conftest import … tree_game` at `tests/test_runner.py:250`; call site `:262` passes `[str(tree_game(fx_stage5_full))]`. Mechanical only — no assertion touched. Consequence verified live: `test_interrupted_run_converges_to_clean_result` now runs its FULL leg (kill caught mid-write, partials parseable, rerun converges hash-equal, zero stray temps) and PASSES — the flagship R10 obligation is executed, not just collected |
| T2 | **APPLIED-CORRECTLY** | `84897a6`: all three label-equality pins removed (`tests/test_stage4.py:89-91` old). Kept/strengthened per option (a): enum membership (`:86`), NEW explicit repeat-call determinism over four shapes (`:97-100`, second-call equality asserted per shape), NEW distinguishability gate — 16 distinguishable compositions may not collapse onto one label (`len(seen) >= 2`, `:110`), exhaustive sweep + all-four-reachable retained (`:106-119`). Semantics NOT pinned anywhere new: grep proves zero `== "<policy-label>"` assertions remain under `tests/`; the spec gained no label definitions (Revision 3 = S1+S2 only); the client-gated leg checks enum membership only (`test_client_gated.py:172-173`) |
| T3 | **APPLIED-CORRECTLY** | `7d2020f`: `"map_catalog_keys"` leads `COVERAGE_NAMES` (`tests/_impl.py:146`); test rewritten as `test_out_of_roster_reference_lands_in_unresolved` driving the REAL seam (`tools/stage2_harvest_catalog.py:86`, signature `(decoded, norm_to_relpath) -> (rows, unresolved)`) with the roster key space built through the real normalizer. Asserts planted bad ref lands in `unresolved` AND `unresolved == {bad_key}` exactly AND one row per slot AND in-roster refs resolve — strictly stronger than the pin. Executes PASSED (no impl-missing skip fires for this symbol anymore) |
| T4 | **APPLIED-CORRECTLY** | `8892e15`: `test_locale_for_bundle_end_to_end_real_bundle_names` (`tests/test_stage0.py:143-164`) drives `locale_for_bundle` (the lookup beside the table, `tools/tpc_common.py:304`) over all 13 real basenames spelled `localisation_assets_localisation_<suffix>.bundle` from `LOCALE_TABLE` — prefix matches the impl's `LOCALE_BUNDLE_STEM_PREFIX` character-for-character — plus unnamed stem → `"base"` with and without extension, plus two non-localisation names → None. Executes PASSED; F1's `lstrip("_")` fix is now regression-walled |
| T5 | **APPLIED-CORRECTLY** | rode `7d2020f`: the `except TypeError:` handler calls `note_missing_symbol("coverage function present but signature differs")` before passing (`tests/test_stage2.py:156`). Verified LIVE in my run's session-end banner: `=== IMPL-MISSING SUMMARY: 0 module(s), 1 symbol(s) === symbol : coverage function present but signature differs`. The present-but-mismatched hook is now counted, not swallowed — and the banner entry is honest: `map_catalog_keys` resolves but the legacy universes-math call shape genuinely doesn't match its signature |
| T6 | **APPLIED-CORRECTLY** | `a2013fe`: `scan_tree_for_media_extensions(ext)` zero-hits assertion added inside the heavy stage-3 leg (`tests/test_client_gated.py:246-249`), placed after carve-out completeness and before uniqueness — closes §5.6's whole-piece acceptance mechanically. Correctly inherits the leg's `TPC_IT_HEAVY=1` gating (this leg is SKIP-ENV on any bare run BY DESIGN, so "executes green" cannot be demonstrated hostless; code inspection confirms the scanner allowlist exempts `media-catalogue.*`) |
| T7 | **APPLIED-CORRECTLY** | `.fixture-trees/probe/` deleted (verified absent on disk; removal, not relayout, as pinned). Suite-owned builder untouched and still proven by `test_fixture_builder.py` |

No DEVIATES, no PARTIAL, no MISSED. Commit `d93588d` deserves note: without
it `test_stage5.py` died at import (`JOIN_NAMES` unbound), which would have
errored the entire stage-5 file out of collection once the adapter
vocabulary landed — fixed minimally (import-only) and transparently.

## Mission confirmations

1. **T2 de-pinned without re-pinning** — confirmed (see T2 row). The only
   compositionPolicy references left in the suite are the four-value enum
   constant (`_validators.py:58-63`) and membership/equality-of-two-calls
   checks; nothing asserts which input yields which label, and the spec's
   §7 "not pre-pinned" posture survives intact. QUESTION-QUEUE routing of
   the labels question is the owner-side follow-up the ruling ordered.
2. **The four formerly loud-skipped obligations execute green** — confirmed
   by individual verbose re-run, 8/8 PASSED:
   - stage-2 out-of-roster → `map_catalog_keys` seam
     (`stage2_harvest_catalog.py:86`, bound by T3);
   - stage-3 reconciliation + completeness → F11-extracted pure helpers
     `reconcile_counts` / `check_carveout_completeness`
     (`stage3_harvest_bundles.py:58/:80`), logic verbatim residual-inclusive,
     binding through `RECONCILE_NAMES`/`MEDIA_COMPLETE_NAMES`;
   - stage-4 matrix builder → F12-extracted `build_locale_matrix(tables)`
     (`stage4_localisation.py:209`), base keys included via the
     `BASE_OVERLAY_NAME` fallback arm of the test.
3. **No masking regressions** — confirmed by diff-checking every touched
   test. Nothing weakened: T2's dropped pins were pins the spec never
   authorized; every replacement assertion is §8-aligned or stronger (two
   NEW gates added). Skips remain loud (all 32 skip sites reason-carrying;
   banner accounting verified live). The three remaining bare `pass`
   handlers are benign and pre-existing: `_impl.call_text_or_path`
   (bytes→path retry ladder, terminal `raise`), `test_stage0` metadata
   reader probe (terminal `AssertionError` on unsupported shape),
   `test_runner._kill` (OSError on already-dead proc).
4. **Suite integrity** — `79 passed / 6 skipped / 0 failed`, exact match to
   the ruling's success signal; no deviation to name.
5. **Coverage-matrix delta vs reviewer-001**:

   | Baseline status | Now |
   |---|---|
   | Stage-2 out-of-roster hard-fail: SKIP-GAP (D3) | **PASS** — real seam bound, exact-set assertion |
   | Stage-3 reconciliation math: SKIP-GAP (D2) | **PASS** ×3 (consistent set + detects gap[manifest]/[catalogue]) |
   | Stage-3 media-completeness checker: SKIP-GAP (D2) | **PASS** |
   | Stage-4 matrix builder incl. base keys: SKIP-GAP (D4) | **PASS** |
   | compositionPolicy classifier: FAIL→TEST (D1) | **PASS** — de-over-pinned, determinism+sensitivity gates added |
   | R10 interrupted-run convergence: FAIL→TEST (D0)+cascade | **PASS** — full kill/converge leg executes |
   | 8 failures (1 impl gap, 6 cascades, 2 test-side) | **0 failures** |
   | 17 skips (6 impl-symbol gaps + 5 stage-5 cascades among them) | **6 skips**, all legitimate env/heavy |
   | Stage-5 bijection/availability legs CASCADE-skipped | **PASS** (stage 5 completes hostless post-F2) |

   Still unproven on this host, unchanged in kind from the baseline: the five
   heavy client-gated legs (by design, `TPC_IT_HEAVY=1`), `make list`
   equivalence (no `make` on PATH), T6's e2e scanner application (rides the
   heavy leg), and impl-side coverage-*universes* artifact math (the
   fixture-level assertions hold; the legacy dict-shape call is loudly
   recorded as signature-mismatched rather than cited as impl coverage).

## Residual list

**R1 — FIX (one line): heavy stage-3 reconciliation assertion lags
Revision 3.** `tests/test_client_gated.py:222` (and its docstring `:194`)
still pins the STRICT identity Σ objectsByClass == manifest + catalogue +
errors. Spec Revision 3 (S1, adopted this round) states the residual-
inclusive form, and the shipped `reconcile_counts` implements exactly that
with `EXPORT_CLASSES = {TextAsset, MonoBehaviour}` and a census-only
residual — on the real corpus GameObject/Transform/manager classes guarantee
residual > 0, so the FIRST `TPC_IT_HEAVY=1` run fails this leg against a
compliant implementation (same defect class as reviewer-001's D1: a test
failing what the contract permits). Fix: add the census-only residual term —
per-class totals outside exported ∪ carved-out — to the docstring identity
and the `==` assertion (the test already computes `census_carved` from
`per_class_total`; the complement is in hand). Pre-existing assertion,
newly invalidat­ed by S1; not a fix-round regression.

Non-blocking notes (recorded, no action forced this round):
- **N1** — T3's docstring says the unresolved→exit-1 hard gate "is covered
  by the runner exit-code tests"; the hostless exit-1 test actually corrupts
  locale-matrix.json (stage-5 input), not a stage-2 reference. The SEAM
  feeding the gate is proven (T3); the gate's own firing rests on reading.
  This matches the ruling's pinned scope verbatim (hostless synthetic
  `catalog.bundle` cannot reach stage-2 decode), but nobody should cite
  more than exists until a UnityFS-writable fixture or heavy-leg run
  closes it.
- **N2** (hygiene) — `.fixture-trees/cg-probe/` (scratch extraction root:
  EXTRACTION-LOG.md, PROOF.md, roster, …) accumulated during fix-round
  probing. Gitignored, unconsumed by the suite (grep: zero references).
  Same class as dismissed-T7; delete on the next hygiene touch.
- **N3** — O4's cosmetic banner wording ("await the CodeWriter deliverable")
  and the matching `_impl.py` module docstring are unchanged; the arbiter
  marked this optional. The accounting itself is accurate and loud.

**Bottom line:** the TEST lane did what the ruling pinned, nothing it
forbade, and the suite now proves every §8 obligation it can prove without
the heavy knob — loudly, deterministically, at exactly 79/6/0. Clear R1
(one line, heavy-leg only) and this section is fully guarded.

END OF testreviewer-002-fixrounds
