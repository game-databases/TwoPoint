# TestReviewer — testreviewer-piece03-build (stage 7 maps suite)

**Seat:** TestReviewer (fresh seat, blind-pair build review). **Date:** 2026-08-26.
**Subject:** `tests/test_stage7_maps.py` + `tests/_mapslib.py` at HEAD
(blind `763b212`, triage `bf13cc6`, reconciliation `b1e2c68`) audited as a
PROOF of [`../specs/piece-03-maps.mdx`](../specs/piece-03-maps.mdx)
**Rev 3** + [`../rulings/arbiter-piece03-spec.mdx`](../rulings/arbiter-piece03-spec.mdx)
(F14–F18 + BINDING R4); implementation cross-read
(`tools/stage7_maps.py`, `tools/maps_util.py`) to verify the tests bite it.

**Method:** every checklist item executed against disk. Suite executed twice
by this seat — fixture-only (`--basetemp=D:/tpc_pytmp/tr03`,
`-m "not client_gated"`): **49 passed · 1 skipped · 0 failed** (skip:
`environment-missing: make not on PATH`); full incl. client-gated
(`D:/tpc_pytmp/tr03full`): **57 passed · 1 skipped · 0 failed** — all eight
real-corpus legs RAN against the landed extraction root and passed. Six
teeth/deletability probes were run as REAL mutations of the implementation,
each restored byte-exact afterward (`git status` clean for every file this
seat touched; NOTHING committed).

---

## 1. AC coverage map (checklist 1)

| AC | Failing leg(s) | Status |
|---|---|---|
| 1 entrypoint / registry | `test_runner_list_enumerates_maps_after_relink` (eight ids, positional order maps AFTER relink), `test_runner_make_list_equivalent`, `test_cli_build_fixture_tree_stage_maps`, `test_runner_only_maps_isolation` (write surface + upstream-deletion guard) | covered (additivity half see MINOR-6) |
| 2 row-count table | `test_fixture_oracle_matches_built_corpus` (oracle RECOMPUTED from built tree — anti-stale), `test_m2_blackbox_row_count_table` (incl. layers==maps pair equality, family decomposition 9/2/1/1/2), `test_client_ac2_row_count_table_holds` (28/51/1031/5188/23004/20/32/4082/54/12 + F16 band + Unused_* survival) | covered |
| 3 coordinate law | `test_m1_unit_constant_parser_over_slice` + `test_m1_unit_parser_drift_flips_value` (mutated slice flips PARSED value), `test_m1_blackbox_coordinate_law_contract` (validate + 27×std + blank 128/10/128 named `Level` + exactly 2 spawn deviants x=−68/y≈2.08646 + multiset provenance), `test_m1_blackbox_parse_not_hardcode_drift` (CellSizeInv→0.25 ⇒ emitted 0.25 AND projection.cellsPerWorldUnit 0.25 AND DRIFT line), `test_m1_blackbox_unparseable_declarations_exit_1`, `test_client_ac3_ac4_chains_verbatim` | covered |
| 4 join contract | `test_m3_blackbox_join_report_arithmetic` (exact fixture arithmetic incl. denominator excludes activation, corroboration partition, F17 cause, measured widening, residue head attribution), `test_m3_blackbox_worked_chain_and_ladder_evidence` (F10 chain verbatim, dstCab+extFileId carried, twin-mismatch recorded, `_raw`→absent, widening resolves residual, dangling-cab + same-file miss ledgered with debug evidence), `test_client_ac3_ac4_chains_verbatim` | covered (see MINOR-1/MINOR-2 on the client half) |
| 5 verbatim sampling | per-row validators on EVERY emitted row (schema + source-provenance complement) + targeted byte-match echoes (`rooms_tiles`/`plots_tiletypes` vs dump payload, `landscape_maps` TerrainMap/ObjectMap verbatim, plot bounds incl. float sentinel in raw bytes, students `_termID 0` sentinel, validator lists verbatim) | partial — the ≤1000→ALL / else-500 sampler with a checked>0 gate is NOT applied to maps artifacts (MINOR-5) |
| 6 determinism | `test_prepared_tree_build_is_byte_deterministic`, `test_float_round_trip_sentinel_is_stable`, `test_m8_blackbox_double_run_byte_identical` (**--force**: genuine re-execution), `test_m8_blackbox_interrupted_run_converges` (truncated finals + stray temps swept, converges to reference), `test_client_double_run_hash_equal` (--force, real tree) | covered |
| 7 carve-out guard | `test_m7_metadata_only_no_bytes_written` (UTF-8-text proof + media-name guard + new-reference scoping) + `test_runner_carveout_guard_green_after_maps_run` | covered |
| 8 ledgers name themselves | `test_m8_blackbox_declared_outputs_and_manifest` (all 24 declared paths exist, manifest digests recomputed, sorted, undeclared-listing forbidden, ALL 37 pinned run keys, absence classes incl. university-out-of-scope + frame-unverified + display-name-unresolved, every row carries unblock), `test_m2_blackbox_identity_collision_demotes_composite`, unresolved-ledger validation | covered |
| 9 exit codes | exit 3: `test_runner_missing_upstream_exits_3` (NAMES externals) + `test_runner_missing_scenario_glob_exits_3`; exit 1: unparseable declarations (names subject); exit 2: steady-state asserted in `maps_run` fixture + `test_runner_exit2_contributors_named` | covered (schema-breakage exit 1 beyond M1 unprobed — MINOR-4) |
| 10 axis openness | `test_m2_blackbox_axis_openness_unseen_values_accepted` (unseen bundle+scenario accepted & emitted; manifest-isolated variable), `test_tools_carry_no_campus_axis_literals` (token-shaped grep over BOTH scripts), fifth-family leg | covered (stage-side refusal arm GAP — MAJOR-3) |
| 11 door hard gate | rule-level: `test_detection_rule_bites_and_spares_containment` + `test_m5_unit_hard_gate_refusal_both_states` (refuse@closed / accept@agreed+links>0 / containment both states); audit: `test_m5_post_hoc_audit_same_rule_over_emitted_artifacts` | **GAP — MAJOR-1**: in-stage WIRING deletable; no emitter-path leg |
| M5 sweeps (G8) | `test_m5_blackbox_id_space_dual_sweep` | **GAP — MAJOR-2**: structure-only; results deletable |
| M6/M7/M1-loadassets | `test_m6_blackbox_named_plots_shared_term_ids`, `test_m7_blackbox_predicates_pinned_and_counted` (patterns literal + counts == fresh oracle over THIS run's catalog), `test_m1_blackbox_loadassets_read_inconclusive` (+ d__NN generic read validated against slice line numbers) | covered |

## 2. Teeth quality (checklist 2) — empirically probed

Six real mutations, targeted legs re-run, tree restored after each:

| # | Mutation | Result |
|---|---|---|
| A | delete BOTH `gate.assert_row_allowed` call sites (write_rows + landscape stream) — the in-stage HARD-GATE wiring | **all 5 M5 legs still PASS** → MAJOR-1 |
| B | `sweep_class_id_spaces` returns `{}` — both G8 sweeps neutered; matched/intersections/bestMatch all collapse to 0/{}, `refsTotal` stays 4 | **all 5 M5 legs still PASS** → MAJOR-2 |
| C | `levelId := payload.get("m_Name")` (F14 reverted) | 3 legs FAIL: `m1_blackbox_coordinate_law_contract`, `m2_blackbox_levels_identity_law`, `m2_blackbox_levels_guid_chain_campaign_parts` — F14 has teeth |
| D | variantOf twin linking deleted | `m2_blackbox_levels_identity_law` FAILS at the twins() helper — R4.2 first-class rows have teeth |
| E | `predicate_matches` → always False (imagery census neutered) | `m7_blackbox_predicates_pinned_and_counted` FAILS — census has teeth |
| F | `generation_for_bundle` INVENTS `levels-prefabs` for unseen stems (closed-vocabulary refusal deleted) | **fifth-gen leg + identity law both PASS** → MAJOR-3 |

State discrimination is distinct and honest: DRIFT (mutated dump.cs ⇒ DRIFT line
AND fresh value wins AND derived projection recomputed), REGRESSION (collision
demotion leg: exit 2 + composites + absence class), stamped-no-op (double-run
and stamp-invalidation legs pass `--force` and say why — the reconciliation
correctly de-vacuumized these). The `maps_run` session fixture asserts
returncode 2 UP FRONT, so systemic stage breakage surfaces as ~30 failures,
never as a shower of artifact-skips.

## 3. Deletability probe (checklist 3) — the four mandated blocks

1. **F14 identity-law block** — delete ⇒ three failing legs (mutation C).
   TEETH PRESENT, hostless and client-side. Caveat: the loud REFUSAL ARM
   (empty-`LevelScene` ⇒ problem ⇒ exit 1) is itself unexercised — the
   fixture never plants an empty scene, and no leg does (folded into MINOR-4);
   the law, not the arm, is what §8 pins.
2. **R4 `variantOf` first-class rows** — delete ⇒ `levels_identity_law`
   fails on BOTH twin pairs (Knight story/remix + ghost pair); the
   no-canonical-flag grep + validator ban back the terminal-policy side.
   TEETH PRESENT.
3. **Door HARD GATE** — split answer. The RULE has unit teeth
   (`door_gate_violation` seam: refuse/accept/containment across both states).
   The IN-STAGE ENFORCEMENT — the pre-write assertion across ALL emitters,
   spec §8: "the emitter path itself refuses a synthetic gated row … with
   exit 1" — has NO failing leg: mutation A removes the wiring and every leg
   stays green. The post-hoc audit cannot compensate: on this fixture NO
   emitted row can ever be gated (validator rows carry no `scenarioName`, so
   the value-resolution arm is unreachable, and RoomType sweep ids are
   deliberately disjoint from room uids 101/102), so the audit is structurally
   incapable of firing. **MAJOR-1.**
4. **Imagery predicate census** — delete/neuter ⇒ `m7` leg fails on missing
   entries or count≠fresh-oracle; artifact deletion trips the m8
   declared-outputs leg (so the `art()` skip path is backstopped). Zero-hit
   minimap negative pinned both sides. TEETH PRESENT.

## 4. Fixture honesty (checklist 4) — 18 spot checks vs the fact base

All matched: F10 chain verbatim (pathId 522383310774550334, `_id`
−572923782, −152+17⇒−135); float sentinel −0.00016784668; F13 shared termID
−1610423369; F11 sample refs [−39494005, −1741033445] + exit pair; spawn
constant/deviants (−66/−68/2.08646 per arbiter probe); bounds 200·16·200 +
blank 128·10·128; generation split 13/9/4/2; CellSize 2/4/0.5/1 + palette
−1/0/1/2/3(+NumTypes, correctly tolerated); census decomposition
22878+86+7+25+8 echoed scaled-and-summing; F17 `_raw`-only absent cause;
F18 errata (d__532, MoveNext RVA 0x8D8D10 / VA 0x1808D8D10, ctor 0x7E2A50,
Dispose 0x669000 — the corrected citations, not the spec's stale ones);
imagery seeds 1094/1140/117/42/66/66/0 + projection 101; P6 30×56 leading
plot layer; twelve-0-dim echoed as one 0-dim DATA row; F16 counterfactual
scaled echo (substring 4/2 vs anchored 3/1); GUID resolvable 27 of 28 with
blank-template nulls. Post-reconciliation dialect matches the landed shapes:
manifest `sourceBundle` spells `<stem>.bundle`, externals keyed by roster
relpath + serialized-file CAB, scenario `_sourceFile` = owning CAB, room
`Anchor` spelled `X/Y` per GridCoord — each with the landed-shape rationale
in a comment. The oracle is internally exact (denominator 13 = 15−2;
match 9 / twin 1 / absent 1 partitions resolved 11) and is RECOMPUTED from
the built tree at test time, killing the stale-oracle trap F14 names.

## 5. Reconciliation strength history (checklist 5) — `bf13cc6` → `b1e2c68`

Compared on seven legs; net STRONGER, no weakening found:

1. M1 provenance: name-keyed dict → multiset `(levelName, pathId)` Counter —
   closes the exact twin-collapse trap the change names.
2. Determinism legs (hostless + client + stamp): second run now `--force`,
   converting a stamped no-op (vacuous hash equality) into genuine
   re-execution.
3. Bitmap validators gained the required identity-key check
   (`uniqueId`/`plotUniqueId`) per the M2 sketches.
4. Client ac3/ac4: exact pre-ladder seeds replaced by ladder-mandated
   DIRECTION teeth (resolved>22210, residue<786, exact decompositions, full
   ledger-row validation). Justified — but see MINOR-1/MINOR-2: one added
   assert is a tautology and the LaunchPad head-name pin vanished outright.
5. Axis leg now registers the unseen-axis dump, isolating the axis variable
   from the unregistered-provenance gate (comment admits that gate exists —
   yet no leg exercises it, MINOR-4).
6. Client ghost-pair law: hardcoded `levelId == "GhostsLevel"` →
   generation-based law (same family + one shared LevelScene + two pathIds),
   which is the correct R4/F14 form and avoids baking a campus literal.
7. CAB evidence match case-corrected to the simplified lowercase house
   spelling (neutral fix, comment documents it).

Skip hygiene: every skip observed is loud and classified
(`environment-missing`, `client_gated`, `wiring-pending`, `artifact-missing`,
`impl-shape` with reasons); markers registered under `--strict-markers`;
reconciliation also BROADENED `_unit` resolution (both pinned scripts +
`tc`/`mu` scopes), removing a whole class of false impl-missing skips. No
silent weakening detected on any compared leg.

## 6. Findings

**CRITICAL:** none.

**MAJOR-1 — In-stage HARD-GATE enforcement is deletable; the §8-mandated
emitter-path fixture is missing.** Spec §8 M5: "the emitter path itself
refuses a synthetic gated row … with exit 1 while instanceLinks.measured ==
0". The suite tests the rule FUNCTION (`door_gate_violation`), never the
WRITER that must invoke it. Mutation A (both `gate.assert_row_allowed` call
sites removed from `tools/stage7_maps.py`) ships green through all 58
collected legs. The AC11 post-hoc audit leg cannot catch it because the
fixture universe provably contains no gated row (validator emissions carry no
`scenarioName`; sweep ids are disjoint from room uids), so the audit is a
structural no-op here — the second check AC11 requires has never been able to
fire.

**MAJOR-2 — The G8 dual id-space sweeps have zero result-level teeth.**
Mutation B (`sweep_class_id_spaces` → `{}`) leaves every leg green: the
recorder leg validates only key presence and enum values. Nothing pins
`fullSpaceSweep.matched` (fixture truth: `{"TPC.RoomType": 2,
"TPC.RoomTypeBuilding": 1}` over refsTotal 4), `coverageUnion` (3),
`integerSweep.bestMatch` (exit-refs ∩ RoomType union = 1), nor — the load-
bearing F12 premises — `roomInstanceIntersection == 0` /
`floorAreaIntersection == 0`, client-side either (`>=71 or >0` is the only
numeric contact). The sweeps ARE the G8 substance; they can vanish silently.

**MAJOR-3 — The stage-side fifth-generation refusal is deletable, and the
black-box leg that targets it passes vacuously.** Mutation F (unseen bundle →
invented `levels-prefabs`) fails nothing. Two compounding causes: (a) the
hospital fixture dump is unregistered, so the manifest-provenance gate rejects
it BEFORE the generation vocabulary is ever reached — the leg exercises the
wrong gate; (b) its fallback branch asserts bare `"DRIFT" in combined`, which
the fixture satisfies on every run via the scale-drift line (scenarios 2 vs
seed 51), independent of any family-named drift. The schema-validator arm
(`validate_levels_row` rejects a fifth VALUE) is tested, but the impl-side
closed-vocabulary refusal — the arm R5/M2 rely on when Medical School lands —
is silently removable.

**MINOR-1 — Tautological assert** in `test_client_ac3_ac4_chains_verbatim`:
`head["count"] == max(r["count"] for r in scenarios)` compares the max row's
count to the max — always true. Dead tooth introduced at reconciliation.

**MINOR-2 — Residue-head attribution-by-name lost at reconciliation.**
`head["scenarioName"] == "LevelScenarioV2_LaunchPad"` (400–520) was dropped
with the CAB-ladder rationale; defensible, but no leg anywhere now attributes
residue to a NAMED scenario (the fixture leg checks only that one scenario
holds the whole residue). G10-adjacent attribution is untested.

**MINOR-3 — The "ONE mechanically pinned detection rule shared verbatim"
is two implementations.** Suite audits with `_mapslib.gated_relation_violation`
(substring key tokens, GLOBAL room-id set, scans the `source` subtree);
stage gates with `mu.DoorGate.violation` (exact key intersection, PER-SCENARIO
id sets, excludes `source`). They agree on every row this corpus can produce,
but the letter of M5/F6 is unmet and the pair can drift; no conformance leg
runs both over a probe-row corpus.

**MINOR-4 — Unexercised loud arms:** unregistered-dump provenance (problems →
exit 1 — fired incidentally in the fifth-gen leg, which never asserts rc) and
the empty-`LevelScene` F14 refusal branch. Both deletable without a red leg.

**MINOR-5 — AC5's systematic verbatim sampler is absent.** No ≤1000→ALL /
else-500 sampled byte-match with a checked>0 gate runs over maps artifacts
(piece-1's sampler covers stub ids only); verbatim coverage is spot-based
(e.g. ONE `rooms_tiles` row and ONE placement leaf byte-compared hostless).

**MINOR-6 — AC1's additivity half untested:** "stages 0–6 outputs byte-
identical before/after registration" has no leg; `--only` write-surface
isolation stands in (reasonable cost trade-off, worth recording).

**MINOR-7 — `slidingDoorComponents`** (seed 37) is asserted only as log-key
presence; the oracle's fixture value (0) is never used, so the counter could
mis-count silently within the fixture's power.

## 7. Verdict

The suite is genuinely load-bearing where it bites: the F14 identity law, the
R4 variantOf policy, the imagery census, determinism/--force discipline, the
join ladder, ledgers and exit-code mapping were all proven by real mutation,
not asserted; fixtures are hand-computed, dialect-clean post-reconciliation,
and the oracle is rebuilt from the tree at test time. What blocks APPROVE is
one enforcement chain whose wiring is unverifiable (MAJOR-1) and two
contract-bearing computations that can vanish without a red leg (MAJOR-2,
MAJOR-3).

**GAPS-REQUIRED**

- **T-1 (MAJOR-1):** Add the emitter-path gate leg the spec pins: drive
  `tools/stage7_maps.py::write_rows` (or the landscape streaming writer)
  directly with a synthetic gated row (validatorId + roomUniqueId in one row)
  against `mu.DoorGate.closed(...)` and assert `StageError` exit 1 NAMING the
  emitter; repeat with the gate open (agreed + links>0) and with a
  containment row in both states. Deleting the `assert_row_allowed` call
  sites must go red.
- **T-2 (MAJOR-2):** Pin sweep RESULTS: extend the fixture oracle with
  `matched == {"TPC.RoomType": 2, "TPC.RoomTypeBuilding": 1}`,
  `coverageUnion == 3`, `integerSweep.bestMatch == 1`,
  `roomInstanceIntersection == 0`, `floorAreaIntersection == 0`; client-gated,
  assert both intersections == 0 and `coverageUnion >= 60` of `refsTotal`
  (band, drift-wins). Neutering `sweep_class_id_spaces` must go red.
- **T-3 (MAJOR-3):** Register the fifth-family LevelConfig in
  `export-manifest.jsonl` (axis-leg precedent) so it reaches the generation
  vocabulary, then assert the pinned outcome (exit 1 naming the family, or a
  DRIFT line naming the family stem) — and tighten ALL bare
  `"DRIFT" in combined` assertions to require the SUBJECT token in the same
  line, so scale-drift lines cannot satisfy them.
- **T-4 (MINOR-1/2):** Delete the tautology; restore residue-head attribution
  as a named-scenario DRIFT-band check (or record its deliberate removal in
  the leg's comment).
- **T-5 (MINOR-3):** Add a table-driven conformance leg running BOTH rule
  implementations (`ml.gated_relation_violation` vs `mu.DoorGate.violation`)
  over a probe-row corpus (substring vs exact keys, source-subtree collision,
  per-scenario vs global ids) and assert agreement — or document the intended
  divergences in both docstrings.
- **T-6 (MINOR-4/5):** Plant one unregistered dump expecting exit 1 naming it,
  and apply the piece-1 sample policy (≤1000→ALL else 500, checked>0) to the
  maps artifacts client-gated.

T-1/T-2/T-3 are the gates; T-4/T-5/T-6 are ride-along repairs in the same
touch. Re-review after landing may be a diff-only pass against §6.

END OF testreviewer-piece03-build
